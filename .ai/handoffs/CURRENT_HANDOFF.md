# Current Handoff Status

**✅ Fourteenth session — closed the one still-open review-integrity gap from yesterday, plus a search-filter robustness fix, 2026-09-19.** Owner returned, asked to keep improving the app. A fresh scoping pass (re-cross-referencing every server.ts route against this doc) found the remaining unaudited surface is thin; two real findings, both fixed.

- **Fix — `/api/chats/mark-picked-up` allowed a buyer to skip the seller's delivery confirmation (`7d0c72a`).** Yesterday's §19 entry flagged this exact gap but left it unfixed pending verification that a server-side block wouldn't disrupt a legitimate in-progress trade — that verification is what today's pass did. Traced both platforms' real UI: web's `ChatInterface.tsx` only renders an enabled "Mark as Picked up" button when `currentStatus === 'delivered'` (otherwise a disabled "(Locked)" button with a "Waiting for the seller to confirm delivery" tooltip); mobile's `ChatsScreen.tsx` only renders the action at all under the identical condition. Since the new server-side precondition (`deliveredBySeller === true`) is exactly what already gates the legitimate button on both platforms, it cannot reject any real in-app confirmation — it only closes the direct-API bypass that let a buyer reach `tradeStatus: 'completed'` (trusted by `/api/reviews/create` as proof of a genuine trade) without the seller ever confirming delivery.
- **Fix — `/api/admin/users/search`'s raw search term reached a PostgREST `.or()` filter unescaped (`7d0c72a`).** `,`/`(`/`)` are structurally significant in that filter grammar; a naturally punctuated admin search (e.g. a phone number formatted `"(024) 123-4567"`) would silently break the filter and fall through to a weaker fallback search with no indication why. No privilege-escalation risk (already admin-only, `select('*')`, same data already available via `/api/admin/users/list-full`) — a robustness/usability fix, not a security one. Periods left untouched (already handled correctly by PostgREST's parser, confirmed via how real email searches already work).
- Two lower-value items surfaced by the same scoping pass, checked and deliberately left as-is: `/api/admin/users-count`'s `Math.max(count, 72)` floor is a pre-existing display-only magic number on an admin dashboard stat, not a functional bug worth churning; `/api/send-welcome-email`'s non-admin self-send path was spot-checked and found correctly scoped.

`tsc --noEmit`, production build, and the full `node --test` suite (46/46) clean.

**Follow-up — owner stepped away for a second unattended "patrol the code" stretch, same day, 2026-09-19.** Two background audits: (1) a search for OTHER "server has no previous-state check, but client UI already enforces the order" gaps like `mark-picked-up` above — came back clean, nothing new found, confirmed that bug class is now genuinely closed out (followed a lead it surfaced anyway, see below); (2) a fresh, skeptical re-read of mobile's two largest/most central files (`SellScreen.tsx`, `firebase.ts`) — found one real gap.

- **Verified clean (no fix needed) — `trustBoostFields` self-boost-grant protection, still fully intact.** The agent's negative-result sweep surfaced a side-note from an earlier session's deferred list: "boost-related product fields... not yet re-examined for a parallel bypass — UNKNOWN." Traced directly: this was already a P0 fix from a prior session (`.ai/handoffs/SUPABASE_DIRECT_ACCESS_AUDIT.md §15`) — `upsertProductToSupabase`'s `trustBoostFields` parameter defaults to `false`, and both regular product endpoints (`/api/products/create`, `/api/products/sync`) call it without passing `true`, so client-supplied `boostStatus`/`boostPriority`/etc. are silently ignored and the real DB row's values are preserved instead. Only `/api/verify-payment` (post-payment) and `/api/admin/boost-control` (admin-only) pass `true`. Confirmed all 4 real call sites are correct — added a regression test locking this in, since it had zero direct test coverage before now.
- **Flagged, explicitly NOT fixed — suspension is enforced entirely client-side, no server endpoint checks `isSuspended`.** Resurfaced by the same sweep; this is a known, previously-documented gap (§14 of this doc's own history) already explicitly classified by an earlier session as "needs a product decision, not a mechanical one" — not an oversight. Left untouched this pass: it's a genuinely cross-cutting, large-blast-radius change (would need to touch many endpoints or add a shared gate, with real open questions like whether a suspended user should retain read-only access, whether admin impersonation needs to bypass it, etc.) that a broad "make recommended decisions" mandate doesn't cover — this specific one still needs Vincent's own call, exactly as the prior session concluded.
- **Fix — mobile `SellScreen.tsx`'s `resetForm()` left category/condition/brand/negotiable/location stale for the next listing (`6182286`).** Same bug class as the already-fixed `93c730f`, just not fully closed by it: `resetForm()` cleared title/price/description/images/video/isExchangeable/postOption/postFlow but never brought itself up to parity with the tabPress listener's own already-correct full reset (category/condition/brand/negotiable/region/city/neighborhood/Services sub-fields). A seller publishing one listing then immediately starting an unrelated one on the same screen instance could silently have the new listing inherit the old one's category and location. Fixed by extending `resetForm()` to the full field set and making it the single source of truth — both the tabPress listener and `handleDiscard`'s own separately-drifting partial reset now call it instead of each maintaining their own copy, closing the root cause (three independently-maintained copies) rather than just the two known instances.
- **Fix — web `ListingModal.tsx`'s `postOption` ("Boost Listing" checkbox) was never reset (`3ac2867`).** A follow-on sweep checking web's equivalent listing-creation modal for the same drift bug class. Both of `ListingModal`'s reset paths already correctly clear every other field, but neither ever called `setPostOption('normal')`. A seller checking "Boost Listing" for one listing, then completing or cancelling it and creating a different, unrelated listing on the same reused modal instance, would find "Boost Listing" already pre-checked — silently pre-selecting the paid boost checkout flow for a listing they never intended to boost. Not a silent charge (checkout still requires completion), but an easy-to-miss surprise. Fixed both reset paths.
- **Fix — mobile `BoostModal.tsx` never reset `paymentMethod` between products (`417b577`).** The mobile twin of a bug already fixed on web's `BoostModal` (`90612e1`). Confirmed reused across every product the same way (mounted once, toggled via props, never unmounted between sessions) — `paymentMethod` was the one field its reset effect never cleared. Picking "Free Admin Boost" for one product then opening the modal for a different, unrelated product reopened it with "Free Admin Boost" still pre-selected instead of defaulting back to Mobile Money.
- **Follow-on reset-drift sweep (admin moderation dialogs, report flows, email composers) came back clean** — the security-hold reason uses a native, stateless `window.prompt()` (nothing to carry over); the report modal on both platforms already explicitly resets `reportReason`/`reportComment` on open; no admin personal-email-composer UI exists in the codebase to check. This lens is now considered run dry after 4 real findings across web+mobile today.
- **Perf fix — the follower/saver notification fan-out in `/api/products/sync` dispatched one notification at a time instead of batching (`479aa16`).** A sequential `for...of` loop with `await` inside meant a seller with hundreds of followers took hundreds of serialized round trips (already fire-and-forget, so no user-facing latency impact, but pure wasted time before the background task finished). Confirmed safe to parallelize: both `shouldNotifyUser()` and `createNotification()` are already internally fault-tolerant, no per-item ordering/error dependency existed. Added a small `dispatchInBatches()` helper (25-at-a-time concurrent batches, not an unbounded `Promise.all`, to cap how many simultaneous Supabase requests one seller's fan-out can trigger) and wired it into both call sites.

- **Fix — mobile `getBoostEndDate()` (`mobile/src/utils/boost.ts`) had drifted well behind web's equivalent (`5dfb9ec`).** Found while setting up mobile's first real unit test infrastructure and reading the file closely enough to write real tests against it — a genuine drift bug, not just a missing-test gap. Missing three things web's `src/utils/dateParser.ts` version has: (1) field-name aliases (`boostExpiry`/`lastBoostedAt`/`lastBoostPurchase`, all fields the server actually writes) — only checked the exact `boostEndDate`/`boostStartDate` names; (2) a robust date parser — used a bare `new Date(...)`, so a Firestore Timestamp object would silently fail to parse (mobile already has an equally-robust `parseDate()` in `tenure.ts`, just never wired into `boost.ts`); (3) an entire fallback tier — web still derives an end date from `createdAt` + plan duration when `boostStatus`/`isBoosted` is true but no date fields exist at all, mobile had no equivalent. Net effect: a real product hitting any of these gaps would show as boosted on web but not mobile for the same underlying data, affecting real ranking/display on `HomeScreen`, `FeaturedListingsScreen`, `SellerProfileScreen`, `ProfileScreen`, and `BoostModal`. Ported verbatim from web's implementation.
- **New — mobile's first real automated test suite** (`boost.test.ts`, `formatPrice.test.ts`, same `node --test --experimental-strip-types` pattern already proven for web's `server.test.ts`). `formatPrice.test.ts` imports and tests the real `formatPrice.ts` directly; `boost.test.ts` mirrors the real logic (same Node-native-ESM-loader extension limitation `server.test.ts` already documents). `mobile/tsconfig.json` now excludes `*.test.ts` from the main `tsc` check (no Node ambient types in `expo/tsconfig.base`'s scope, and adding them project-wide risked exposing Node-only globals to the app's own RN code) — tests still run and assert correctly regardless. 17/17 pass.
- **Fix — three more web-parity drift bugs found continuing the same cross-platform sweep (`bd62b13`).** `productImage.ts`'s `filterValidImages` never excluded `unsplash.com`, despite this file's own header comment documenting that "showing an unrelated stock photo as the seller's actual photo" is exactly the bug class it exists to prevent — verified real (not theoretical): `SellScreen.tsx` substitutes a hardcoded Unsplash stock photo for a photo-less, video-less listing, and `/api/products/sync`'s own response echoes that raw value straight back before any server-side cleanup on a later GET would strip it. `discoverSellers.ts`'s location resolution was missing the `region` field web's equivalent checks first, and its `sellerListingCounts` lookup was missing the `displayName`-keyed check web's `getRealCountForUser` has — both real, verified discrepancies for the same underlying data. Both `Product` type imports switched to `import type` (correct regardless of testing — a type-only usage in both) — added real/mirrored tests for both files (17 more tests, 45 total in mobile's suite now).
- **Checked and confirmed NOT a live issue — `discoverSellers.ts`'s broader `isVerified` check (5 field aliases vs. web's canonical single-field `isUserVerified` in `src/types.ts`, which only checks `emailVerified`).** Verified directly: `idVerified`, `verified`, and `badge === 'verified'` have zero real writers anywhere in `server.ts`, web, or mobile — confirmed via grep, only ever read, never set (matches an already-known, previously-flagged `matchedUser?.isVerified` dead-field pattern in `getSellersSummaryData`). Functionally equivalent to checking just `emailVerified` in real production data today; not touched.
- **New — real unit tests for `src/utils/dateParser.ts`** (`150c5e9`), the exact reference implementation mobile's `boost.ts`/`tenure.ts` were ported from/verified against today — 15 tests, zero coverage before now despite backing real boost-ranking/display logic. Its `Product` import switched to `import type` (a genuine correctness fix, not just for testability), which turned out to also make it directly importable by Node's stripped-types test runner where `productUtils.ts` (a mixed type+value import of the same path) still can't be. 66/66 web suite pass.
- **Fix — `normalizeServerProductSummaryRow`'s `boostPlan` fallback disagreed with its sibling function (`3a60a92`).** A fresh re-run of the "cross-check the same field's fallback across all product-serialize functions" technique that caught the original price/condition bugs earlier this session. `normalizeServerProductRow`/`serializeProductSummary` (backing the single-product page) default `boostPlan` to `'7days'` for a row that's actively boosted but has no stored plan — a real, confirmed-non-hypothetical shape (`getServerBoostEndDate`'s own `isBoostedFlag` branch computes exactly this). `normalizeServerProductSummaryRow` — the function actually backing the primary `/api/products`/`/api/feed`/`/api/featured`/`/api/trending` feeds via `getProductsListData()` — still defaulted to `undefined` instead, meaning the identical row's real ranking (via `productSelector.ts`'s boost-priority tiebreaker, which ranks `'7days'` above `undefined`) literally depended on which endpoint served it, for the exact bug class `0ed3bb7` was meant to close for good. Fixed to match the sibling's exact fallback expression.

`mobile tsc --noEmit` clean. Session continuing.

**✅ Thirteenth autonomous session — X-Forwarded-For rate-limit bypass fixed, plus a full email-privacy investigation → fix cycle, 2026-09-18.** Started as a scoped, interactive (not unattended) engagement, then extended into another unattended "improve the app" stretch through 5pm.

- **Fix — rate-limiter/view-cooldown IP derivation trusted a spoofable `X-Forwarded-For` (`55dd825`).** Confirmed live production topology (`Client → Cloudflare → Render LB → Express`, Cloudflare fronts every Render web service unconditionally) and that both hops only *append* to an existing `X-Forwarded-For` rather than overwrite it — so `.split(',')[0]` always read an attacker-controllable value, defeating every IP-keyed rate limit in the file (exactly the risk this doc's Twelfth-session-era entries had flagged but left unfixed pending real topology confirmation). Switched `serverRateLimiter()` and the product-view cooldown to Cloudflare's own `cf-connecting-ip` header (TCP-verified, non-spoofable), no `X-Forwarded-For` fallback, no `trust proxy` change. Verified live in production (two requests, differing spoofed XFF, same real IP → correctly bucketed together).
- **Investigation → fix — unauthenticated/bulk email exposure across `/api/sellers`, `/api/sellers/summary`, `/api/sellers/counts`, and `/api/users/get` (`c61051e`).** A multi-stage, read-only-then-approved-then-implemented investigation (seller-identity matching, production data sampling, call-site tracing on both platforms) found: `getSellersSummaryData()` put every active seller's email directly into `/api/sellers`'s public response AND as literal dictionary keys in the `counts` object (a second exposure vector, easy to miss since it's a key, not a value); `/api/users/get` returned any user's email to any caller, authenticated or not. Confirmed via live production data (110 products, 20 users) that canonical `sellerId`→`users.id` matching is 100% sufficient today — the client's email-fallback branches for seller-listing counts are effectively dead code. Fixed: removed email (value + both dictionary-key sources) from the seller-summary path; `/api/users/get` now returns email only to the record's own authenticated owner (tolerating the one known legacy `user_<uid>`-formatted account id, confirmed live in production data, so that account's self-lookup isn't wrongly treated as "another user"); replaced the two pre-auth `/api/users/get?username=/&phoneNumber=` calls in `loginUser` with a new, minimal `POST /api/auth/resolve-login-identifier` (email-only response, 15min/10-request rate limit) so the necessarily-unauthenticated login-resolution step no longer returns a full user row. `phoneNumber`/`whatsAppNumber` deliberately untouched (sellers publish these for buyer contact by design). A pre-commit review caught and fixed one real regression before it shipped (the legacy `user_<uid>` self-lookup gap above) — verified via new focused unit tests, not just code inspection.
- **`admin_audit_logs`: design and full implementation review are both approved (docs only, nothing executed)** — `ADMIN_AUDIT_LOGS_SCHEMA_PROPOSAL.md` (shape) and `ADMIN_AUDIT_LOGS_IMPLEMENTATION_REVIEW.md` (exact SQL/code diff review, all three previously-open points now signed off) are both up to date. Per standing discipline, no `CREATE TABLE` or code changes have been made — awaiting an explicit go-ahead before executing either.

`tsc --noEmit`, production build, and the full `node --test` suite clean after every commit this round.

**Follow-up — owner stepped away for a 5-hour unattended "improve the app" stretch, same session, 2026-09-18.** A scoping pass (grepping every file/route never named by this doc) plus a dedicated Paystack/boost-payment correctness audit surfaced five more real, verified findings, each fixed as its own isolated, tested commit.

- **Fix — `/api/verify-payment` trusted the request's own `planId` instead of what was actually paid for (`36af981`).** The existing amount check only verified the paid amount covered the *client-claimed* plan's price, so a request could pay for an expensive plan (e.g. `1month`, GHS 10) then call verify-payment with a cheaper `planId` (e.g. `3days`, GHS 1) and be awarded only the cheap plan's shorter duration/lower priority — the amount check passes either way. No platform-loss or cross-user exploit (the reverse was already blocked), but a real way a paying customer could end up with less boost than paid for. Fixed the same way the amount itself is already trusted: derive the effective plan from Paystack's own transaction metadata (set immutably at `/api/paystack/initialize-boost` time, echoed back by Paystack's verify endpoint) instead of the request body. `/api/admin/boost-control`'s separate, fully-admin-trusted `planId` handling is untouched. A dedicated fresh audit of the rest of the Paystack flow (webhook signature, replay protection, amount trust, client success-path gating) came back otherwise clean — no webhook actually exists (everything gates through the authoritative server-to-server verify call), replay protection is DB-level (not just in-process), and amount is already cross-checked against Paystack's own verified figure.
- **Fix — `normalizeProduct()` (client) re-introduced two already-fixed server-side bugs (`d2a8030`).** Never named in this doc (unlike the server-side serializers), found via a scoping pass for genuinely unaudited files. `price: Number(rawProduct.price)` unconditionally coerced through `Number()`, which is `NaN` for any literal price phrase ("Inquire", "Contact for Price") that every Services/Jobs & Employment listing sends by design (`Product.price` is deliberately `string | number`, per the server-side fix `3a6ad43`) — `ProductCard.tsx`'s own formatter checks `typeof === 'number'` before `'string'`, and `NaN`'s typeof is `'number'`, so this rendered literally "GHS NaN", worse than the original "GHS 0" bug it was supposed to have already fixed. `condition` fell back to `'Used - Good'`, not one of the four real selectable presets — the exact non-preset value already standardized away to `'Slightly Used'` server-side in `e61f4d1`, never applied here. Added a `normalizeClientPrice()` helper mirroring `normalizeServerPrice()` exactly; confirmed safe against every real consumer (no unguarded numeric arithmetic on `.price` anywhere in `src/`, `productSelector.ts` already has its own correctly-typed `parseNumericPrice()` for ranking).
- **Fix — password-hash and shared-secret comparisons used plain `===` instead of constant-time (`fe47ff3`).** `/api/auth/verify-and-sync-password`'s PBKDF2 hash comparison (plus its legacy-plaintext fallback) and the `CRON_SECRET` header check at `/api/admin/backfill-video-eager` both used `===`, a textbook timing side-channel on a password-verification code path. Added `timingSafeStringEqual()` (wraps `crypto.timingSafeEqual()` with a safe length-mismatch guard) and wired into all three sites. No behavior change for any legitimate request.
- **Fix — `/api/admin/send-personal-email` interpolated username/message/subject into HTML with no escaping (`acd9c3c`).** Same bug shape as the registration-OTP/welcome-email fix (`f8713ab`), missed by that sweep. Admin-only, but `displayName` is the target user's real stored username auto-passed in by the admin panel — a legacy username predating the registration-time `<`/`>` block (which only gates on the username *changing*, never retroactively cleans existing rows) could still carry raw HTML. Escaped the raw message once up front, then substituted the also-escaped `displayName` into its `[user name]`-style placeholders (safe against double-escaping); escaped `emailSubject` for the `<title>` tag.
- **P0 fix — both product-delete routes' ownership check failed OPEN, not closed, on a lookup failure (`eda55e9`).** The most severe finding of this stretch. `POST /api/products/delete` and `DELETE /api/products/:productId` both looked up the target product's `sellerId`, swallowed any query error (`catch (_) {}`), then only enforced ownership when `sellerId` was truthy: `if (sellerId && !isOwner && !isAdmin) return 403`. A failed lookup (transient Supabase error, or a row with no sellerId) left `sellerId` null, made the whole condition false, and skipped the check entirely — any authenticated non-admin could delete ANY product. Fixed both identically: a real query error now returns 500 instead of silently proceeding; a genuinely nonexistent product returns 404; the ownership check is no longer gated on `sellerId` being truthy, so a row with a missing sellerId now correctly denies instead of bypassing.
- **Fix — the same fail-open shape in `/api/products/sync` and `upsertProductToSupabase`, higher stakes (`3e85700`).** A dedicated sweep of the codebase's remaining `catch (_) {}` blocks for the same pattern just found in the delete routes. `/api/products/sync`'s own existing-row lookup silently swallowed query errors too — a failed lookup made the handler treat a real, other-owned listing as "brand new" (`isExistingProduct = false`), skipping the ownership check AND letting the eventual upsert (keyed by id at the DB level, independent of what the handler "thinks") silently overwrite that listing's entire row and `sellerId` with the attacker's own submission. Not reliably triggerable on demand (a transient query error isn't attacker-controlled), but the same "availability problem must never become an authorization bypass" principle applies. Fixed identically (throw/500 on a real error, still correctly treat a successful "no row found" as a legitimate new listing) in both the route and `upsertProductToSupabase`'s own separate internal lookup (used by every caller) as defense-in-depth — traced all current callers (`/api/products/create`, `/api/verify-payment`) and confirmed none were independently exploitable today, since each already pre-computes a safe sellerId before calling it, but the silent catch made that an undocumented, fragile assumption. The remaining `catch (_) {}` blocks in the file (Firebase Admin startup, a password-reset existence pre-check, three SSR/meta-tag read-only rendering paths) were checked and confirmed to gate no authorization/mutation decision — left as-is.
- **Chat trade-completion routes (`/api/chats/mark-delivered`, `/api/chats/mark-picked-up`) audited, checked out clean.** Specifically re-checked against the exact fail-open shape above: both re-derive participant membership from a fresh DB row keyed on the Firebase-verified uid (never client-supplied), and a failed lookup correctly returns 404 (fail closed), not a bypass. **Flagged, not fixed**: `mark-picked-up` sets `tradeStatus: 'completed'` unconditionally, without checking `deliveredBySeller` was ever set first — a buyer can unilaterally jump straight to "completed" (which `/api/reviews/create` trusts as proof of a genuine trade) without the seller's stepper step ever happening. Not a participant-bypass (the buyer is a legitimate chat participant either way), but a real review-system-integrity gap; not fixed because verifying whether the client stepper UI already gates this action on `deliveredBySeller` (making a server-side block redundant vs. a real behavior change for an in-progress legitimate trade) needs a live browser/device check this sandbox can't do.
- **Fix — `SellingBuyingSettingsTab.tsx` never resynced its `role` state when `currentUser` changed (`ce37cd2`).** Found via the same audit; its sibling `NotificationSettingsTab.tsx` already has the equivalent resync effect. Mirrored it exactly.
- **Fix — footer "Terms of Service" link's direct `window.location.hash =` write desynced browser history for the NEXT navigation (`0b44225`).** Found and fully traced (not guessed) via a dedicated audit of two never-reviewed client files, `useHashRouting.ts` and `videoCache.ts`. A direct hash assignment fires an async `hashchange` event deferred to a later browser task; `useHashRouting`'s own effect reacts to the `currentView` change synchronously first and overwrites the hash to `#/settings` (it has no concept of `/terms` as a sub-path). When the original deferred `hashchange` finally fires, the hash already matches React state (no-op), but it unconditionally sets `isPopStateRef.current = true`, which nothing resets until the hook's effect dependencies next change — so the very NEXT unrelated navigation anywhere in the app gets misread as Back/Forward and silently skips `pushState`/`replaceState` (UI renders fine, but URL/browser history stop updating for that one navigation). Fixed at the single call site with `window.history.replaceState()` instead, which sets the hash synchronously (still correctly seeding `ProfileSettings.tsx`'s one-time mount-time tab read) but never fires `hashchange` — the shared hook itself is untouched. Not live-browser-verified (no browser access here), but the fix substitutes a well-documented browser API behavior (replaceState never fires hashchange, per spec) rather than guessing, and is scoped to the one call site that was bypassing the hook rather than touching its shared core logic.
- **Fix — `videoCache.ts`'s `offlineBlobMap` had no eviction, unbounded memory growth during long scroll sessions (`74341d3`).** Same audit. Its sibling `dataUriToBlobMap` is correctly capped/evicted at 50 entries; `offlineBlobMap` (populated on every video that becomes active or gets preloaded in the video feed) had no cap and no `URL.revokeObjectURL` anywhere — a genuine leak in exactly the long-scroll scenario this file's own header comment targets. Fixed with the identical bounded eviction the sibling map already uses; safe against evicting an actively-playing video's blob since `VideoAdsFeed` mounts at most ~19 videos concurrently, well under the 50-entry cap. Two smaller, lower-severity findings from the same audit deliberately left undone: an opaque `no-cors` fetch response is cached without a status check (an inherent limitation of `no-cors` mode — the response's real status literally can't be read, not something a code fix can close without a broader redesign) and no in-flight-request dedup for concurrent cache attempts of the same URL (wasted bandwidth only, not corruption).
- **Mobile audit — `TabBarVisibility.tsx` and `EmailVerificationModal.tsx` checked out clean; `SuspensionGate.tsx`'s Dismiss button could race ahead of the actual sign-out (`5ac24e1`).** Three never-previously-reviewed mobile files. First two: no stuck-hidden tab-bar path, no missing-await/catch, no double-submit risk — clean. `SuspensionGate`: `logOut()` (fire-and-forget, itself awaiting a native Google Sign-In call before the real Firebase `signOut`) could take several seconds or hang; the Dismiss button previously re-rendered the full app instantly regardless, so a suspended user could tap Dismiss before the real sign-out landed and keep using the app — defeating this gate's whole purpose for as long as they kept dismissing (this gate is already documented, same as web's equivalent, as a client-side UX layer rather than real server enforcement — but the web equivalent's own "make the exit path actually reliable" gap was already treated as worth fixing on those same grounds, not dismissed as moot). Fixed by only letting Dismiss take effect once the triggered sign-out attempt has actually settled (success or failure, matching the existing tolerance), showing "Signing out…" meanwhile. Deliberately narrow — no change to the auth-state-transition or navigation logic around it, which can't be verified without a live device.

- **Fix — two admin write endpoints reported success on an unchecked Supabase error (`4de495a`).** A fresh scoping pass through the admin mutation cluster for the same shape found `POST /api/admin/accounts/security-hold` (Supabase `.update()` wrapped in `.catch(() => {})`, directly inconsistent with its own sibling `/api/admin/users/suspend` two routes below which already correctly checks `error` and throws) and `POST /api/admin/users/delete` (the final, highest-stakes `users` row delete wrapped in a `try/catch` that Supabase's non-throwing `{error}` shape never actually triggers). Both now check the returned `error` explicitly and throw, matching the already-correct sibling pattern. No behavior change for a successful write. (`/api/admin/boost-control` was checked too — already correctly fixed in an earlier session, per its own in-code comment; `send-personal-email` doesn't write to the DB at all, not applicable.)
- **Fix — `/api/users/merge-account`'s old-row cleanup delete had zero error visibility (`b6c1178`).** A broader grep for the same unchecked-write pattern. Unlike the merge-upsert just above it (already correctly checked/thrown), the old account row's delete after a successful merge had no error handling at all — a silent failure here left the old row lingering indefinitely with the same email as the now-merged new row, a real duplicate-account data-hygiene risk with zero trace in the logs. Not escalated to a hard failure (the merge itself already genuinely succeeded by that point, so the response's success claim stays accurate either way) — just added the missing `console.warn`, matching this endpoint's own tolerant cascade-step pattern.
- **Fix — a systematic, file-wide sweep found four more real instances of the same shape (`8397f1d`), closing this bug class out.** `deleteProductFromBackend()` (shared by both product-delete routes, the admin bulk-delete cascade, and the 30-day sold-listing retention purge) only `console.warn`'d on a real deletion error, never signaled it — both direct delete routes (whose ownership check was fixed for the identical fail-open shape earlier today) unconditionally reported "deleted successfully" while the listing stayed live. Now rethrows; the admin bulk-delete cascade's loop (which previously relied on this function swallowing failures to keep going across products) got its own per-item try/catch so one failure no longer aborts the rest of that user's product cleanup. `/api/auth/delete-account`'s CASE B tombstone write (the step that actually overwrites real PII with anonymized placeholders — the closing step of a GDPR-style deletion) and CASE A's security-hold freeze write (meant to lock a fraud-investigated account into `under_investigation` rather than let it escape review, previously an entirely empty, unlogged catch) both had the same gap — both now checked and thrown. A minor fifth item (the same cascade's store-name release, whose response specifically promises "store name released") got the missing visibility without escalating to a hard failure, preserving that step's own established best-effort tolerance.

`tsc --noEmit`, production build, and the full `node --test` suite (42/42) clean after every commit this round. Session continuing.

---

**✅ Twelfth autonomous session — CRITICAL unauthenticated stored XSS on every product page, fixed (`c2d08bc`), 2026-09-18.** Owner extended the mandate past the original 5-hour window to run through security/UI/egress until 7am. Found while re-checking `injectMetaTags` (the product-page SSR meta-tag injector) for the same escaping pattern already confirmed correct on its sibling `injectSellerMetaTags`.

- **The most severe finding of the entire session.** `injectMetaTags` builds a `<script type="application/ld+json">` JSON-LD block from `product.title`/`description`/`sellerName` via plain `JSON.stringify()`, which never escapes `<`. A listing whose title or description contains a literal `</script><script>...</script>` closes the JSON-LD tag early and injects an attacker-controlled script into the page — and this is real, server-rendered HTML served on every actual visit to that product's page (the SSR meta-tag injection path), not just a crawler/social-preview artifact. Unlike every other finding this session, this required **no authentication, no admin access, and no specific-victim targeting** — creating one ordinary listing with a crafted title was enough, and every visitor to that listing's page would execute it. Fixed by replacing every literal `<` in the JSON output with its 6-character JSON escape sequence before embedding it in the script tag — verified this round-trips identically for any real JSON-LD/schema.org parser, so nothing changes for legitimate consumers. Grepped the whole file for other `application/ld+json` usages: this was the only one.
- **A dedicated, careful follow-up sweep of the rest of `server.ts` for the same bug shape found two more real instances (`f6bbe2a`), one arguably worse than the original.** The SPA catch-all's homepage pre-warm scripts (`window.__INITIAL_PRODUCTS__`/`__INITIAL_SELLER_COUNTS__`/`__INITIAL_DISCOVER_SELLERS__`) have the identical unescaped-`JSON.stringify()`-in-`<script>` bug, carrying raw product title/description/location/brand and seller username/displayName/location straight through — this fires on `/` itself, the single highest-traffic page, rather than only individual product pages. Fixed with the same escape. Also found a genuine **reflected** XSS (not stored): the catch-all's fallback canonical-link branch (any path that isn't `/product/...` or `/seller/...`) interpolated `req.path` — attacker-controlled via the URL itself — unescaped into an `href` attribute; a crafted link decoding to `"><script>...` would inject a script for anyone who clicked it. Every sibling canonical/og:url built from request data elsewhere in the file already used `escapeHtml()`; this was the one path that didn't. Also defensively escaped `generateDownloadPageHtml`'s host-derived URLs (same shape, lower-confidence reachability depending on whether the front-end proxy validates the `Host` header before this process sees the request — fixed regardless). Confirmed via a repo-wide grep that `dangerouslySetInnerHTML` is used nowhere on either platform, and independently re-verified `src/utils/sitemap.ts`'s XML generation was already correctly escaping everything.
- Found this while chasing a smaller, related discovery first: the registration-OTP and welcome-email HTML templates interpolate a client-supplied username with no escaping (fixed, `f8713ab`) — this file's own `injectSellerMetaTags` already correctly wraps every field in `escapeHtml()`, so checking `injectMetaTags` (its product-page sibling) for the same pattern was the natural next step, and it's where the JSON-LD gap turned up instead of a repeat of the same email-shaped bug.
- Also root-caused the email finding one layer deeper: `/api/users/sync` never validated username characters server-side at all (`621ba79`) — only client-side. Deliberately did NOT reuse the client's strict `[a-zA-Z0-9_-]` regex, since Google sign-in legitimately sends a raw `displayName` (e.g. "John O'Brien", spaces and punctuation) that never passes through that validator — traced the actual sign-in flow before writing the fix rather than assuming, and caught that the strict regex would have broken real registrations. Shipped a narrower check blocking only literal `<`/`>`, gated on the username actually changing so existing/legacy usernames never block an unrelated profile save.

`tsc --noEmit` and `npm run build` clean after every commit this round.

**Follow-up — the bundle-size audit's one flagged-not-fixed opportunity turned out safe to do (`553737f`).** That pass found `AdminUserManagement` still eagerly bundled into `ProfileSettings.tsx`'s chunk for every user (not just admins) but held off fixing it without checking whether its two render sites had animation/layout-timing assumptions. Checked both directly: plain `{condition && <Component/>}` renders, nothing Suspense-unsafe. Lazy-split it the same way `App.tsx` already does for named exports, each usage wrapped in its own small local `Suspense` (a spinner) rather than relying on `ProfileSettings`' own outer Suspense boundary, which would otherwise show a jarring full-page loader for this tiny sub-panel. Measured: `ProfileSettings-*.js` 165.12kB → 150.08kB (34.86kB → 31.74kB gzip).

---

**✅ Eleventh autonomous session — P0 account-takeover fix in `/api/auth/confirm-password-reset`, 2026-09-17.** Continuing the same unattended "5 hours, improve every aspect" stretch. Found while sweeping server.ts endpoints not yet covered by this session's audits.

- **P0 — full account takeover by email address alone, no proof of ownership required at all (`6501a2d`).** Two independent bypasses in this one handler: (1) a request body of `{ clientConfirmed: true, email: "<victim>", newPassword: "<attacker's choice>" }` with **no token whatsoever** set the target email directly and proceeded straight to overwriting that account's real password (both the Supabase `password_hash` and the actual Firebase Auth password via Admin SDK) — zero verification of any kind. (2) Even when a `token` *was* supplied but failed real Firebase verification (any non-empty garbage string satisfies the code's only check on it, `typeof token === 'string'`), the handler fell back to an Admin SDK lookup keyed purely on the client-asserted `email` field and unconditionally overwrote that account's password anyway — Admin SDK has no method to verify a password-reset oobCode, so this "fallback" verified nothing at all; it just needed the victim's email. Grepped every real caller (`ResetPasswordModal.tsx` is the only one, both platforms) and confirmed the app itself always sends a genuine Firebase oobCode as `token` and never sends `clientConfirmed` — meaning both bypasses were dead weight for legitimate use and pure attack surface. Fixed by removing both paths entirely: the only route to a changed password is now `confirmFirebasePasswordResetViaRest(token, newPassword)` succeeding, which atomically verifies the real oobCode against Firebase's own REST API and sets the real Auth password in one step. No functional change for any real caller — confirmed via the grep above before removing anything.
- **P1 — `/api/users/merge-account` authorized a full account merge on an unverified email string match (`dfc6e73`).** This endpoint (used when a returning user's sign-in resolves to a new Firebase uid but a matching old account already exists) fetched the caller's real `emailVerified` flag from Firebase Admin, but only ever used it as metadata for the merged record — never as a gate. The actual authorization check was just `oldRow.email === verified.email` (a string match), and Firebase's `createUserWithEmailAndPassword` lets a brand-new account claim *any* email string with zero proof of ownership (`emailVerified` starts `false` until the address is actually confirmed). An attacker could sign up claiming a victim's real email and use this endpoint to inherit the victim's entire account — products, chats, username all reassigned to the attacker's uid, with the original row permanently deleted — without ever receiving or clicking a verification email. The endpoint's own existing comment already documented the *intended* model as "caller's own Firebase-verified email"; the fix just makes the code actually enforce that. No client change needed — `AppContext.tsx`'s only caller already handles a refusal by falling back to creating a fresh account.
- Endpoint discovery method for this session: diffed `server.ts`'s full list of `app.get/post/...` route registrations against every filename/route-fragment already mentioned in this handoff doc, to find the handful of endpoints no prior audit this session had actually opened. `/api/sellers/counts`, `/api/products/:productId/view` (self-view exclusion + per-IP cooldown, non-atomic increment already covered by the session's existing documented-not-fixed `viewsCount` finding), `/api/admin/support/chats`, `/api/admin/accounts/deleted`, `/api/auth/check-store-name/:username`, and `/api/admin/retention/run-purge` were all read and confirmed clean/already properly `verifyAdmin()`-gated where relevant.

`tsc --noEmit` and `npm run build` clean after every commit this round.

**Follow-up — background audit of admin/access-control components, one real fix (`21e434c`).** `AdminSecurityGate.tsx`, `ImpersonationBanner.tsx`, `VerificationBlockModal.tsx`, `AdminUserManagement.tsx` all confirmed already correctly hardened (client-side gates are explicitly documented UX-only, with every real action behind an already-verified server endpoint). `SuspendedBlockModal.tsx`'s "Log Out & Exit" used a hand-rolled partial cleanup that never cleared the impersonation-session localStorage keys, unlike the shared `logoutUser()` every other sign-out path uses — meaning it could reload back into a stale, still-active impersonation state instead of a genuinely logged-out app. Fixed by routing through `logoutUser()`. Two items found but explicitly left as documented-not-fixed product decisions (same shape as the session's existing deferred "suspension enforced client-side only" item): email-verification blocking is also purely cosmetic client-side (no server endpoint checks `emailVerified` before letting an unverified user post listings/start chats/leave reviews); and `users.isAdmin`'s DB column has no real effect on server authorization today since `setCustomUserClaims` is never called anywhere in the codebase, so a second "delegate admin" account would show a fully-functional-looking panel while every real action silently 403s.

**Follow-up — extended past the original 5-hour window: owner explicitly instructed continuing until 7am across security/UI/egress, 2026-09-18.** Continuing the same autonomous stretch under that extended instruction.

- **Fix — `/api/auth/check-store-name/:username` had no rate limiting at all (`55fb3de`)**: the only data-querying endpoint in the whole file with zero `serverRateLimiter`. Unauthenticated, up to two real DB round trips per call (Supabase then a Firestore fallback), no caching. Grepped both `src/` and `mobile/src/` and found no client caller at all today — a standing username-enumeration/DB-hammering vector with no real traffic pattern to preserve.
- **Perf/egress — `GET /api/reviews` had no caching, unlike every sibling read-heavy endpoint (`00c8383`)**: the no-`sellerId` case (the global "fetch once on mount" reviews state) returns every review ever created, unpaginated, with zero caching, while `/api/sellers`, `/api/search/suggestions`, `/api/featured`, etc. already use the same `serverCache`+ETag 60s pattern. Added it here too, invalidated immediately in `/api/reviews/create` (both the per-seller and unscoped keys) so a just-submitted review is still visible right away rather than waiting up to a minute.
- **Fix — seller dashboard/profile loading skeletons didn't match their real layouts (`4b7e324`)**: `SellerDashboardSkeleton` showed an 8-card square grid; the real "My Listed Ads" tab is a `grid-cols-12` table (thumbnail+title/category/views/actions) — since the component is lazy-loaded, a slow connection saw the skeleton then a visibly different layout snap into place. `SellerProfileSkeleton` rendered one full-width 4-across grid with no room reserved for the real page's `lg:grid-cols-12` 8-col-catalog/4-col-sidebar split, causing a reflow on mount. Both rebuilt to mirror their real shapes.
- Re-verified the endpoint sweep is now complete: every remaining unaudited `app.get/post` route (`/api/sellers/counts`, `/api/products/:productId/view`, `/api/admin/support/chats`, `/api/admin/accounts/deleted`, `/api/admin/retention/run-purge`, `/api/sitemap/clear`, `/api/users/push-token`, `/api/users/heartbeat`, `/api/welcome/setup`, `/api/products/invalidate-cache`, `/api/paystack/initialize-boost`, `/api/paystack/callback`) read and confirmed clean/already correctly gated. Also re-checked the client-side admin-PIN hardcoded `'2330'` fallback flagged much earlier this session — confirmed it was already fixed in a prior pass (explicit comment: removed as an always-accepted bypass, now purely UX friction with every real privileged action independently re-verified server-side) and Firebase's client API key in `mobile/src/firebase.ts` is the standard, intentionally-public per-project identifier, not a leaked secret. No other hardcoded credentials found in a targeted grep sweep.
- **Process note**: two background audits' unreviewed, concurrent edits to `server.ts` got bundled into one of my commits via a blanket `git add server.ts` while a background agent was still writing to the same file. Caught before pushing — split back into two proper, individually-attributed commits (`55fb3de` for mine, `00c8383` for the egress agent's) via `git reset --soft` and manual re-staging, nothing lost. Going forward this round: `git add <specific file>` only, never a blanket add, whenever a background agent might still be writing to the working tree.

**Follow-up — unescaped usernames could inject raw HTML into email templates, plus the storage-time gap that let a malicious one exist at all (`f8713ab`, `621ba79`).** The registration-OTP and welcome-email HTML templates interpolate `displayName` (client-supplied `username`) directly into HTML with no escaping — this file's own `injectSellerMetaTags` already wraps every field in `escapeHtml()` first, these two just didn't. Fixed both (registration-OTP's "Hello X," line, welcome-email's preheader and heading); left the plain-text bodies/subject lines alone since escaping those would show literal `&lt;` instead of plain text. Both templates always email the same account whose username is embedded, so today's practical exploit is limited to a user breaking their own inbox's rendering — not demonstrated to reach a different victim — but fixed anyway as defense in depth. Root-caused further: `/api/users/sync` (the chokepoint behind every registration/profile-save) never validated username characters server-side at all — only `registrationValidation.ts`'s client-side regex did, trivially bypassed via a direct API call. Deliberately did NOT reuse that client regex (`[a-zA-Z0-9_-]` only) — Google sign-in (`AppContext.tsx`) sends the raw `firebaseUser.displayName` as the initial username, which legitimately contains spaces/punctuation ("John O'Brien") and never passes through that validator, so reusing it server-side would have rejected real, working registrations (caught this before shipping by tracing the actual Google sign-in flow, not assumed). Added a narrower check blocking only literal `<`/`>` — enough to close HTML-tag injection at the point of storage without touching any legitimate name — gated on the username actually changing so an existing user's own untouched (possibly pre-fix, possibly Google-derived) username never blocks the rest of their profile save.

**Follow-up — P1: non-numeric prices ("Inquire", "Contact for Price") silently became "GHS 0" everywhere (`3a6ad43`).** `src/types.ts`'s `Product.price` is explicitly typed `string | number` — a deliberate, first-class feature: `ListingModal.tsx`/`SellScreen.tsx` both let a seller type a literal price phrase, and critically, **every** Services and Jobs & Employment listing on both platforms always sends the literal string `"Inquire"` as its price, never a number, by the client's own design. Every place in `server.ts` that touched price forced it through `Number()` unconditionally — `Number("Inquire")` is `NaN`, which serializes to `null` over the wire to Supabase (`price` is a plain `TEXT` column per `supabase_schema.sql`, so storing a string was always schema-safe — the bug was purely in this file's handling), and reading `null` back through `Number(null) === 0` meant every such listing silently displayed "GHS 0" everywhere — product cards, detail page, SSR meta tags, search results — instead of "Inquire" or whatever text the seller actually entered. Added a `normalizeServerPrice()` helper (verified against every real input shape before wiring in) that normalizes a price value the same way the client's own submit logic already distinguishes numeric vs. literal-text prices, and wired it into all three places that previously called `Number()` directly. **Important limitation, cannot fix from here**: this stops the corruption for every write *going forward*, but any listing that already has `price: null` stored in production from before this fix has permanently lost its original text — there's no way to recover what the seller originally typed. Those listings will keep showing "GHS 0" until the seller edits and re-saves that listing themselves (which will now persist correctly). No way to know how many existing rows are affected without direct DB access; Vincent may want to run a one-time query for `products` rows with `price IS NULL` to gauge scope, though likely concentrated in Services/Jobs & Employment listings specifically since those are guaranteed-affected.

**Follow-up — inconsistent `condition` fallback across the three product-serialization functions (`e61f4d1`).** Applying the same "cross-check the same field's fallback across all three product-normalize functions" technique that found the price bug above: two functions fell back to `'Used - Good'` when a row's condition is falsy, one fell back to `'Slightly Used'` — and `'Used - Good'` isn't even one of the four real, selectable condition presets on either platform (`['Brand New', 'Slightly Used', 'Refurbished', 'Used - Fair']`, confirmed identical in `ListingModal.tsx`/`SellScreen.tsx`). The same underlying row could show a different condition depending on which endpoint served it. Standardized all three on `'Slightly Used'`, the one that actually matches a real preset. Systematically re-checked every other shared field (`brand`, `subcategory`, `location`, `category`, `negotiable`, `isExchangeable`) across all three functions afterward — found one more benign difference (`brand`/`subcategory` fall back to `null` on write vs `''` on read) but confirmed it has zero observable effect since the read-side already normalizes either to `''` before a client ever sees it, so left untouched. Also independently re-confirmed the `|| true` "isVerified" anomaly in `getSellersSummaryData` is the exact same already-known, deliberately-deferred instance documented in `SUPABASE_DIRECT_ACCESS_AUDIT.md` (not a new finding) — no action needed.

**Follow-up — web search suggestions never had a popular-keywords seed list, unlike mobile (`34e7be6`).** Found via a dedicated search-autocomplete ranking-correctness audit, same lens as the boost-ranking one. `getPrefixAutocompleteSuggestions` (shared, byte-identical logic on both platforms) has a step that matches the typed query against a seeded list of common-search phrases so a query still surfaces a helpful suggestion even with sparse inventory for that term — mobile's `SearchScreen.tsx` defines and passes a 14-phrase list into this; web's `SearchSuggestions.tsx` called the same function with no seed list at all, silently disabling that entire branch on web only. Typing e.g. "office" or "elitebook" could surface a suggestion on mobile and nothing on web, same shared engine, no legitimate reason for the divergence. Fixed by adding the identical (byte-for-byte) list to web. Also traced the `boostPriority` field flagged by an earlier pass as "typed but never read" in `searchAutocomplete.ts` — confirmed it's genuinely dead/superseded (never populated by either server serialization function; the real boost comparator in `productSelector.ts` derives priority from `boostPlan` instead), correctly not wired in. Flagged, not fixed: `viewsCount` is live data reaching this function but never used as a scoring tie-break — real but low-severity (only reorders among already-exact-score ties), and picking a tie-break weight with no spec to anchor it was judged a "matter of taste" call, left to a deliberate follow-up if wanted.

**Follow-up — P1: two real bugs undermining the paid-boost feature's core promise, found via a dedicated ranking-algorithm correctness audit (`0ed3bb7`).** Different layer from this session's earlier boost-payment/activation-race fixes — this is about the *ranking computation* itself, never audited for logic correctness before. (1) `boostPlan` (e.g. `'1month'` vs `'3days'`) was computed inside both `normalizeServerProductSummaryRow` and `serializeProductSummary` but never actually included in either function's returned object — every client-facing endpoint built on these (`/api/products`, `/api/feed`, `/api/featured`, `/api/video-ads`) delivered `boostPlan: undefined` for every product. `productSelector.ts` (web + mobile) uses exactly this field as its top-priority tiebreaker among boosted listings ("higher-value package wins regardless of remaining time") — with it always missing, a seller on the GH₵10 1-month plan with 1 day left could rank *below* a seller on the GH₵1 3-day plan who'd just bought it, even though the pricier package is supposed to win outright. Fixed by returning the field from both functions. (2) `/api/products`/`/api/feed` never sort at all — they only filter+slice whatever the shared product-list cache returns, which was ordered purely by `createdAt DESC` with no boost awareness, so a listing boosted today but created weeks ago could sit past the page-1 window (web locks pagination after one fetch of the default homepage view) and never reach the client at all — meaning the client-side boost-sort had nothing to reorder, because the boosted item was never loaded in the first place. Fixed by sorting the one shared cached array boost-active-first (ties broken by recency) at its single source, confirmed not to affect the three sibling endpoints that already do their own explicit re-sort for different, legitimate reasons. Also fixed an incidental crash-safety gap in the same area (a Firestore-fallback path could push a `null` row into an array it then sorts, throwing and silently emptying the whole feed — added a `filter(Boolean)`).

**Follow-up — password-reset log lines leaked the raw reset link, a live secret (`7e77472`).** Two rare-path `console.warn` calls in `/api/auth/send-password-reset` logged the raw Firebase-generated password-reset link verbatim whenever URL parsing unexpectedly failed to extract its `oobCode` — but that raw link already contains a real, live, usable reset secret as a query param regardless of whether this code could parse it back out. Same secret-hygiene shape as this session's earlier `redactUserSecrets` work, just in a log line instead of an API response. Now logs only that parsing failed, never the link contents. Found via a targeted sweep for this exact bug shape (raw secret/credential objects logged verbatim) across the rest of `server.ts` — checked every other `console.log/warn/error` call touching `req.body`, a full `user`/`cleanUser` object, or a raw Firebase/Supabase response `data` object near password-reset code; confirmed this was the only real instance (a sibling function's similarly-shaped failure-path log turned out safe on inspection — it only fires when no link was ever generated in the first place, so there's nothing in scope to leak).

**Follow-up — web UI-polish pass, same lens as the mobile one, two real fixes (`399a19a`).** `SellerDashboard.tsx`'s Saved (watchlist) tab derives its list from the shared `products` context array but had no loading/error differentiation — the sibling Listings tab in the same file already distinguishes fetch-error/still-loading/genuinely-empty; while `products` was loading or failed, the Saved tab showed the same "Your Saved Ads are Empty" copy as a real empty watchlist. Added the same two branches the Listings tab already has, reusing its exact styling. `ForYouListingsView.tsx` (the "View all" page for the homepage `ForYouSection` carousel) also derives entirely from that shared array with no fetch of its own, but unlike siblings `FeaturedListingsView.tsx`/`TrendingListingsView.tsx` (which gate on their own loading flag with a skeleton grid) had no loading gate at all. Added the identical skeleton pattern. Checked and confirmed already fine: `ChatInterface.tsx` doesn't have the mobile-equivalent "reset to `[]` on switch" bug (global state only clears on logout, not on switching between open chats); `SellerProfilePage.tsx` already has empty states on every tab; the AI style-picker's sizing/double-submit protection in `ListingModal.tsx` already matches established patterns; every modal close button already has adequate padding (the web equivalent of mobile's hitSlop fix). One thing found but deliberately not touched: `SellersDiscoveryView.tsx` has the same shape of missing-loading-gate, but both it and its sibling already merge SSR-seeded data (`window.__INITIAL_DISCOVER_SELLERS__`) before the live fetch resolves — adding a gate on top felt more likely to cause a skeleton-flashing-after-real-content regression than fix a real gap, medium confidence without a browser to verify.

**Follow-up — mobile UI-polish pass (a different lens from the earlier correctness-only mobile audit), eight real fixes (`946e3e9`).** `ChatsScreen.tsx`'s messages list reset to `[]` on every thread open, briefly flashing the "Fully Encrypted Chat" empty-state copy before the first fetch resolved — added a loading gate. `FollowersFollowingScreen.tsx` had no loading gate at all (every sibling list screen has one), rendering "Not following anyone"/"No followers yet" immediately before real data arrived. `SellerProfileScreen.tsx`'s Reviews tab had no empty state at all — the common case (a seller with zero reviews) showed a blank area, while the Listings tab right next to it already had one; added the matching empty state reusing the same styles. `ProfileScreen.tsx`/`ProfileStoreSettingsScreen.tsx` (stacked-input forms right above a submit button) had no `KeyboardAvoidingView`, unlike `ChatsScreen`/`SellScreen` which already establish that pattern for the same shape of form. `SellScreen.tsx`'s AI-description style-picker pill (added earlier this session, never visually re-checked) was visibly smaller than the screen's own established chip sizing for the same toggle affordance. Four small circular modal-close buttons across `HelpSupportSettingsScreen.tsx`/`ProductDetailScreen.tsx` had no `hitSlop`, unlike the app's own established pattern for that exact button shape elsewhere. All verified against an existing in-app pattern before fixing, not invented from scratch. `mobile tsc --noEmit` clean.

**Follow-up — mobile-wide audit found nothing new (mobile is now thoroughly covered).** Cross-referenced every file in `mobile/src/context/`, `mobile/src/components/`, `mobile/src/screens/`, `mobile/src/utils/` against this doc; the ~10 genuinely never-mentioned files were all pure functions or a single already-hardened global provider (no per-target mutable state to leak, no network calls to race) — confirms diminishing returns on further mobile sweeps at this point.

**Follow-up — bundle-size investigation, one small verified win (`72a5e0a`).** `FeaturedListingsView`/`TrendingListingsView`/`ForYouListingsView`/`SellersDiscoveryView` were still eagerly imported into `App.tsx`'s main chunk despite being rendered only conditionally inside the exact same `Suspense` block that already lazy-loads 9 other same-shaped components — `FeaturedListingsSkeleton` even already existed and was already wired into the Suspense fallback for one of them, suggesting this was intended but never finished. Converted all 4 to `lazy()`, matching the existing pattern exactly: ~14.5kB off the mandatory first-load path. Investigated and confirmed NOT worth changing: `heic2any` (already correctly dynamically imported, deferred until a HEIC file is actually uploaded — apparently already fixed earlier this session), `framer-motion` (already isolated to a shared chunk only lazy views pull in), `firebase`/`dompurify`/`@supabase/supabase-js` (all core, eagerly-needed for first paint, not safely deferrable). One real but not-attempted-blind opportunity: `AdminUserManagement.tsx` bundles into every user's `ProfileSettings` chunk even for non-admins — plausible to further lazy-split, but needs a closer read of its two call sites (possible layout/animation timing assumptions) before doing it without a browser to verify.

**Flagging, not fixed — `serverRateLimiter` trusts the client-supplied `X-Forwarded-For` header directly, with no `app.set('trust proxy', ...)` anywhere in the file.** Every rate limit added or relied on this session (including today's own `/api/auth/check-store-name` fix) is keyed on `req.headers['x-forwarded-for'].split(',')[0]` — the FIRST comma-separated entry. If the real deployment has exactly one trusted reverse-proxy hop that *appends* its own IP to an existing header rather than overwriting it (the standard, spec-correct proxy behavior), a client can prepend an arbitrary fake IP before the request ever reaches that proxy, and this code still reads that attacker-controlled first value — completely defeating every rate limit in the file, and (unrelated to rate limiting) any other place elsewhere in the file that uses this same `clientIp` pattern for logging/keying (e.g. `/api/products/:productId/view`'s cooldown). Deliberately NOT fixed blind: whether this is actually exploitable depends entirely on the real production deployment topology (how many trusted proxy hops sit in front of this Node process, and whether that proxy overwrites vs. appends the header) — information this sandbox has no way to check. Guessing wrong here isn't just a missed fix; setting `trust proxy` to the wrong hop count, or switching to Express's own `req.ip`, could cause a *worse*, immediately-live regression (all traffic behind a shared proxy silently bucketed under one rate-limit key, real users getting 429'd because of others). Vincent should confirm the actual number of trusted hops (check whatever's in front of this Node process — a platform load balancer, Cloudflare, nginx, etc.) and either configure `app.set('trust proxy', N)` plus switch `serverRateLimiter` to use `req.ip`, or confirm the current behavior is already correct for the real setup.

**Flagging, not fixed — no visible deployment path for `firestore.rules` changes.** Checked this since it's the RLS-equivalent access-control layer for the Firestore side of the data model (Supabase's own RLS-disabled situation is already the subject of a whole separate blocked-approval doc). `firestore.rules` itself reads as a thorough, well-hardened ruleset (per-collection, per-field validation; boost/ranking fields explicitly protected from owner edits; terminal-state locks on completed trades) — better than I expected going in. But: there's no `.firebaserc` anywhere in the repo (the file that normally holds the Firebase project id for CLI deploys), `firebase.json` only has a `hosting` block with no `firestore` rules-deploy target at all, and there's no deploy script in `package.json` referencing it either. `git log -- firestore.rules` shows real, substantive edits to this file this session (e.g. "security: remove dead serverVerificationSecret bypass from firestore.rules") — meaning either those fixes never actually reached the live Firestore project (if rules are deployed by copy-pasting into the Firebase Console, disconnected from this repo entirely), or there's a deploy mechanism that exists outside this sandbox that I simply can't see. I have no Firebase CLI login/credentials here to check what's actually live, so this is a flag, not a fix: **Vincent should confirm whether `firestore.rules` in this repo actually matches what's deployed in the Firebase Console right now**, and if not, either deploy it (`firebase deploy --only firestore:rules`, which will need a `firestore` block added to `firebase.json` pointing at this file, plus a `.firebaserc` with the project id) or set up whatever the real deploy path already is so future edits here aren't silently inert.

**Follow-up — general HTTP security-header hardening sweep.** `server.ts`'s existing header middleware (CSP, `X-Frame-Options`, `nosniff`, referrer-policy, permissions-policy) had no `Strict-Transport-Security` at all — added, gated on `NODE_ENV === 'production'` (matching the file's own existing convention) since HSTS is aggressively cache-sticky in browsers and sending it from a non-HTTPS local/dev server could permanently break `http://localhost` for whoever hits it next (`ec08fe9`). **Found but explicitly NOT fixed**: the existing CSP is very permissive — `script-src 'self' 'unsafe-inline' 'unsafe-eval' https:` allows a script from *any* HTTPS origin plus inline/eval, materially weakening its XSS defense. Deliberately did not tighten this: doing so safely requires enumerating every real third-party origin the app actually loads from in production (Google Sign-In popup, Paystack checkout, Cloudinary, any analytics) and verifying in a real browser that nothing breaks — the exact kind of live-verification-required change this session has already learned the hard way not to guess at blind (see the Gemini model-name incident earlier in this doc). Flagged for Vincent's own review/decision, not attempted.

**Follow-up — background egress/performance audit, avatar thumbnailing fixed across both platforms (`eb9561b`).** Both platforms' `cloudinary.ts` already have proven thumbnail-transform helpers, already used correctly for product photos, but a repo-wide grep found avatar `<img>`/`<Image>` tags never called them — shipping whatever resolution the user originally uploaded (commonly several hundred KB to a few MB straight off a phone camera) into a 20-96px circle. Fixed the highest-repeat-count instances: web's `ProductCard.tsx` (seller avatar in every card, every feed/grid/search result site-wide — also added missing `loading="lazy"`), `Navbar.tsx` (header avatar, every page load), `VideoAdsFeed.tsx`; mobile's `SellerCard.tsx`, `ChatsScreen.tsx` (per-row inbox avatar), `ProductDetailScreen.tsx`. Each a one-line src wrap, safe no-op passthrough for non-Cloudinary URLs (Google avatars etc.), same crop the container already applied via CSS — no visual change, only bytes transferred.
- **Also fixed as part of the same audit**: `GET /api/reviews` had no caching at all (already covered above, `00c8383` — the agent independently found and fixed the identical issue concurrently; correctly did not double-apply once it saw the commit land).
- **Found but explicitly deferred as product/architecture decisions, not touched**: (1) `GET /api/users/list` (and mobile's mirror) returns the *entire* `users` table with no `.limit()` at all, polled every 3min (web, on tab-focus) / 60s (mobile, on-foreground) by every signed-in session — the single largest structural egress risk found this session, but several consumers (follow lookups, discover-sellers ranking, online-dot computation) currently assume the full list is present client-side, so a real fix means pagination or moving those lookups server-side, not a safe drop-in. (2) Web's `pollSelfProfile` runs every 5 seconds for a session's entire lifetime (by far the highest-frequency request in the app) — an existing, deliberately-commented tradeoff from an earlier session for near-real-time suspension/verification-status detection; flagged that cutting it to 15-20s (matching the chat/notification cadence) would meaningfully reduce total request volume at the cost of slower live-session updates, a call left to Vincent. (3) A broader avatar-thumbnailing gap in the same shape as the six fixes above exists in `SellerProfilePage.tsx`, `ChatInterface.tsx`, `AdminUserManagement.tsx`, `ProfileSettings.tsx`, mobile `ProfileScreen.tsx` — left alone this pass (lower-traffic pages, or need a per-call-site size judgment, e.g. an edit-avatar preview that's deliberately shown large and shouldn't be thumbnailed down) — straightforward same-shape follow-up if wanted.

**Follow-up — background audit of `MediaRenderer`/filter/MCP-tool components, three real fixes (`e5574d8`).** (1) `DynamicCategoryFilters.tsx` stays mounted across category switches but never reset its open-dropdown/search-query state on `selectedCategory` change — since many categories share field ids (`brand`, `condition`, etc.), leaving a dropdown open with search text typed and switching category reopened it immediately under the new category, pre-filtered by search text meant for a different field's option list. (2) `App.tsx`'s footer "Top Ghana Regions" quick links passed display labels ("Ashanti Region") straight to `setSelectedRegion`, but `productSelector.ts` compares it with strict equality against the canonical, un-suffixed names in `regions.ts` ("Ashanti") — every link except "Greater Accra" silently returned zero results; also didn't reset `selectedCity` unlike every other region-setting call site in the file. (3) `WebMCPInitializer.tsx`'s `search_listings`/`get_listing_details` MCP tools only ever read `productsRef.current` (whatever page(s) of `/api/products` the client had already paginated in, first load `limit=24`) instead of the full catalog their own tool descriptions promise — real, already-working server-side search (`/api/products?q=`) and single-product lookup (`/api/products/:id`) existed the whole time; both tools now try the real endpoint first, falling back to the local cache only on a network error. `MediaRenderer.tsx` itself was confirmed already correctly guarded (resets its error state on `src` change) but is dead code — imported by `ProductCard.tsx`/`ProductDetail.tsx` yet never actually rendered by either, both maintaining their own duplicate inline media-rendering logic instead; flagged, not touched (a real refactor call, not a bug fix). `GhanaLocationFilter.tsx`'s `regionCounts`/`cityCounts` `useMemo`s are computed every render but never referenced in the JSX — likely a removed or half-built count-badge feature; flagged, not wired in (a design decision).

---

**✅ Tenth autonomous session — AI description length/tone options, boost/payment business-logic bugs, listing-fetch race sweep, 2026-09-17.** New unattended stretch, following a live user-reported crash on "Post" (ListingModal) that led to a careful re-read of that submit path (no render-time cause found — an ErrorBoundary only catches render exceptions, and handleSubmit's own try/catch already lives in an event handler/promise, so this remains unconfirmed pending a real stack trace from the user) but surfaced two real, independent state-leak bugs along the way.

- **Feature — AI description length/tone picker (Short/Standard/Detailed), both platforms (`d9976af`).** Requested directly by Vincent after the AI-description-generator fix. `server.ts`'s `AI_STYLE_PRESETS` is the single source of truth for what each style means (word-count range + tone); the system instruction and response schema are now built per-request from it (`buildSystemInstruction`/`buildResponseSchema`) instead of being fixed constants. Web (`ListingModal.tsx`) and mobile (`SellScreen.tsx`) both got a 3-pill picker directly above the description field, wired through the existing generate/regenerate button unchanged.
- **P1/P2 fix — the same Gemini "AI generate description" feature, iterated live with Vincent through three real production error logs in a row**: (1) the model name (`gemini-3.6-flash`, set by an earlier fix after `gemini-2.0-flash` was retired) turned out to still be correct — my own attempt to "fix" it to `gemini-2.5-flash` based on the installed SDK's offline bundled docs was WRONG and had to be reverted (`649a7da`) once a live 404 showed Google's API itself naming `gemini-3.6-flash` as current; lesson applied going forward: never change this default without a live error message in hand, only ever revert based on actual evidence, not documentation that can lag behind Google's fast deprecation cycle. (2) A real, correctly-generated response was being thrown away as "unparseable" because `gemini-3.6-flash` is a thinking model whose internal reasoning tokens ate the `maxOutputTokens` budget before the JSON could close (`0d39dc9`) — fixed by disabling thinking (`thinkingBudget: 0`, unneeded for this task) and raising the ceiling, plus added a best-effort truncated-response recovery as a safety net. (3) A transient Gemini 503 "high demand" overload failed the whole request on the first hit (`f158f2d`) — added a short retry-with-backoff scoped to 503/429.
- **P1 — `ListingModal.tsx` leaked AI-generation state and `isExchangeable` across reused sessions (`5d2ad5f`)**, found while investigating the reported crash. `SellerDashboard.tsx` mounts one `ListingModal` instance unconditionally (same reused-instance pattern already fixed for `BoostModal`/`SellerProfileScreen`/`FollowersFollowingScreen` this session) — `aiDescriptionError`/`aiDescriptionWarning`/`aiDescriptionStyle`/the last-AI-text ref were never reset on reopen for a different listing, and `isExchangeable` was already missing from this same reset effect's create-mode branch even before today (a separate, pre-existing gap).
- **P1 — two real boost/payment business-logic bugs found via a dedicated audit, explicitly scoped away from the already-covered security aspects (`ff3873f`)**: the admin free-boost path never extended from an existing active boost's end date (always reset to `now + durationDays`, silently discarding remaining paid time — the real-purchase path already did this correctly, just not the admin one); and there was no locking at all around the read-existing-boost → verify-with-Paystack → compute-extension → upsert sequence, so two genuine, different payment references for the same product verified in overlapping windows (plausible given the multi-second Paystack round trip) could race, with the loser's paid-for extension silently overwritten. Fixed with an in-process per-productId async mutex — closes the race for this app's current single-instance deployment; flagged (not silently assumed away) that a multi-instance deployment would need a real DB-level lock instead.
- **P1/P2 — stale-response races in Featured/Trending listing fetches, web (`c5a1c14`)**: the homepage carousels' existing cancellation guards only re-checked before the second `await` (`res.json()`), not after, so a rapid category switch could let a stale response land; the "View All Featured" full page had no guard at all. Same fix pattern applied to both, consistent with 4+ other instances of this bug class fixed earlier this session.

`tsc --noEmit` and `npm run build` (both projects where applicable) clean after every commit this round.

**Follow-up — mobile `HomeScreen.tsx` pagination/filter audit completed (retried once after an infrastructure timeout on the first attempt).** One finding, ruled NOT a bug: the home feed's default "All Categories" view is hard-capped at 24 products with no load-more (`isAllCategories`/`onEndReached` at `HomeScreen.tsx:~1222-2600`) — confirmed via `git log -S isAllCategories` that this was deliberately authored by Vincent himself in commit `f72d834` ("restrict footer to home all-categories and implement batch pagination for category product browsing"), not a prior Claude session or a regression. Left untouched.

**Follow-up — two small, previously-deferred mobile `SellScreen.tsx` items picked back up (`42c3fcb`, `46b5c6a`).** Both were noted earlier this session as real but lower-priority than what was being tackled at the time; revisited now that a natural gap opened up. (1) `openCamera`'s permission-denied Alert (shared by both the create wizard and edit mode) was text-only ("enable it in your device Settings") with no actual button to get there once `canAskAgain` is false — matched the same `Open Settings` action the mic-permission Alert right next to it, and the wizard's own dedicated camera-permission screen, already correctly have. (2) Edit mode's "Record Video" always stored `MAX_VIDEO_DURATION_SECONDS` (30s) as the clip's duration regardless of how long the user actually recorded — the gallery-picker path right next to it already used the real measured duration, just not this one. Fixed with a ref (not the existing `recordingSeconds` state, which would still read its stale render-time value after the long-running `recordAsync()` await resolves) tracking the live elapsed seconds, threaded through `openCamera`'s callback signature to `uploadPickedVideo`.

**Follow-up — "Unread" inbox filter tab didn't apply the same completed-trade exclusion as every unread badge, both platforms (`428ebdf`).** Both platforms' core unread computations (web's `chatStateUtils.ts`, mobile's `UnreadChats.tsx`) and every per-row/nav badge consistently exclude a completed-trade chat from counting as unread — a deliberate, multiply-consistent rule on each platform. The "Unread" inbox filter TAB on both platforms independently had the exact same gap (only checked `unreadCount > 0`), which is what confirms it's a genuine oversight rather than per-platform intent. Fixed identically on both. Verified via re-tracing the whole unread-count call graph that this was the only remaining gap (web's `getUnreadMessageCount`, used for the browser-tab-title badge, already had the exclusion correctly; mobile's `UnreadChatsProvider` polling already has a proper generation-ref staleness guard, confirmed clean).

Also discovered (not part of the normal workflow, just noted): `src/utils/chatStateUtils.test.ts` and `authErrorHelper.test.ts` are real, runnable Node native tests (`node --test --experimental-strip-types <file>`) — no `npm test` script wires them up and neither existed for mobile, but both pass as-is and neither exercises the specific logic touched this round.

**Follow-up — `withProductBoostLock` self-verified by manually tracing a concrete two-request race, no issues found.** Given the money-sensitive nature of the boost-payment fix (`ff3873f`, item above), re-read the mutex and walked through it step-by-step against a scenario of two genuine, different `paymentReference`s for the same `productId` arriving with overlapping Paystack-verify round trips: the `Map<string, Promise>` chaining correctly serializes the second request's entire read-modify-write body behind the first's, with no gap between "read existing boost" and "write extended boost" where a second writer could interleave. Confirmed this only protects a single-instance deployment (in-process map), already documented as such.

**Follow-up — `/api/users/sync` silently reported success on a real database write failure, plus a related gap documented (not fixed) for Vincent's review (`717aa8e`).** The `users` table upsert's error was only `console.warn`'d, then the endpoint fell through to an unconditional `res.json({ success: true })` regardless of whether the write actually happened — same "write failure reported as success" shape fixed earlier this session in `deleteAccount`/`adminToggleSecurityHold`/`updateProduct`'s rollback, just one layer deeper here: `registerUser` (`AppContext.tsx`) already correctly rethrows on `!data.success`, but that fix was powerless against this specific failure mode since the server never actually signaled failure. Now returns a real `500` with a message, translating a Postgres unique-violation (`23505`) into "That username is already taken." Tracing this surfaced a genuine, still-open gap: **there is no active username-uniqueness enforcement anywhere today** — not application-level, and (as far as this sandbox can determine without DB credentials) not at the database level either. `store_names` (the lowercased-username reservation table) doesn't prevent it in practice, since its own upsert overwrites the existing row's owner on a matching id rather than rejecting the write. Documented as a schema proposal rather than guessed at with an application-level check (which would need careful, live-tested atomic handling to avoid its own TOCTOU race): `.ai/handoffs/USERNAME_UNIQUENESS_SCHEMA_PROPOSAL.md` — a case-insensitive unique index on `users.username`, with a required first step to check for and resolve any existing duplicate usernames in production before it can be added. Awaiting Vincent's review; nothing executed.

**Follow-up — mobile's two Cloudinary upload functions had no retry, unlike web's own fix earlier this session (`8b4d882`).** `uploadVideoDirectToCloudinaryMobile` and `uploadMediaToCloudinaryMobile` (images + the video relay fallback) both made a single attempt with no retry at all — a transient network blip failed the whole attempt immediately, leaning entirely on SellScreen's own per-item manual "Retry" UI to recover. Web had the identical gap for its direct video path, fixed earlier this session (`5521e93`) to match its image path's existing 3x-with-backoff; mobile never got either function's equivalent. Both now retry 3x with the same 1s/2s backoff — the direct-to-Cloudinary path re-signs per attempt (short-lived signature), the relay path reuses one auth header (safe, not short-lived) across attempts. Confirmed no double-retry conflict with SellScreen's own single-call-then-manual-retry UI layer — this just makes each individual attempt (automatic or user-triggered) more resilient, doesn't duplicate retries.

**Follow-up — a genuinely valuable new lead: diffing web/mobile's supposedly-identical shared-name utility files (`b8f7f7c`).** `filterConfig.ts` exists on both platforms as an intentional "verbatim port" (mobile's own header comment says so) — diffing them directly (not just reading one side) surfaced a real divergence: mobile's `CATEGORY_FILTERS` was only ever keyed under the legacy `'Laptops'` name, missing the canonical `'Laptops & Computers'` key web has (with its own alias). Mobile's own category chips use the canonical name (confirmed via `HomeScreen.tsx`'s own icon map), so the category-specific extra-filter fields (brand/RAM/storage/etc.) never rendered for that one category on mobile at all. The same mismatch in `getModelsForBrand`'s category check was worse than just "no models" — falling through to a generic brand lookup that has separate plain `Apple`/`Samsung` keys for PHONE models meant selecting Apple as the brand while filtering a Laptops & Computers listing showed iPhone models instead of MacBook models. Fixed both by aliasing the keys and adding the dual-name check, matching web exactly. Also spot-checked the other same-named utility pairs: `searchAutocomplete.ts` is confirmed byte-identical logic (only a comment differs); `productSelector.ts`/`recommendationScore.ts`/`cloudinary.ts` differ substantially in size but are legitimately platform-scoped on inspection (mobile's own header comments document the intentional scope reduction), not drift — not worth a line-diff.

---

**✅ Housekeeping — mobile `tsc --noEmit` is now fully clean (`2f3ce6e`), 2026-09-17.** The three `HomeScreen.tsx` type errors carried and re-confirmed as "pre-existing, unrelated" at every check this entire session are now fixed rather than just tolerated. Root cause: the "Video Feed" toggle button lives inside the grid FlatList's own list header, which only renders inside the `viewMode === 'grid'` branch of the screen's main ternary — TypeScript correctly narrows `viewMode` to the literal `'grid'` there, making the three `viewMode === 'video'` comparisons driving that button's active/selected styling statically impossible (and always `false` at runtime too, since the button is reachable only as a switch-TO-video action, never as a "currently selected" state). Removed the dead comparisons rather than casting past them — zero behavior change. Also spot-checked both codebases for the shared TS suppressions (`@ts-ignore`) and confirmed the only two present are legitimate, well-documented runtime/type-definition mismatches, not masked bugs.

Small independent verification pass, also swept for other "collected but never verified" credential patterns (the shape of the account-deletion fake-password-check bug) — grepped every password `<input>` on web and traced each: `Navbar.tsx`'s login/register and `linkGoogleWithPassword` genuinely call `signInWithEmailAndPassword`, and `AccountSecuritySettingsTab.tsx`'s delete-account field is the one already fixed this session. No further instances found. Also spot-checked (read fully, all clean, no changes) five more small mobile screens: `SavedProductsScreen`, `FeaturedListingsScreen`, `TrendingListingsScreen`, `ForYouScreen` (all pure display, delegate writes to already-verified `ProductCard`), and `HelpSupportSettingsScreen` (static content, no network calls at all).

---

**✅ Ninth autonomous session — ChatsScreen trade/review flows, cross-platform review-draft leak, 2026-09-17.** Continuing the same unattended stretch. A background audit of mobile `ChatsScreen.tsx`'s trade-completion stepper and review flow surfaced a bug pattern that, once found, turned out to also exist on web — checked and fixed there too rather than left as a platform gap.

- **P1 — stale review rating/comment leaked across unrelated trades, both platforms (`94951c8` mobile, `2a8a94f` web).** On mobile, `reviewRating`/`reviewComment` were only reset on a *successful* submit — every path that opens the modal (auto-open after confirming pickup, the standalone "Leave Review" button) left a previous draft sitting in state if the user had dismissed it unsubmitted, and it silently carried into the next, unrelated trade's review modal. Checking web's `ReviewModal.tsx` for the same shape found it too: `ChatInterface.tsx` mounts the modal unconditionally for whichever chat is open (not keyed per-chat), so the same persisted-draft-across-trades bug existed there. Since reviews are public and tied to seller reputation, submitting a stale draft against the wrong seller is a real trust-signal bug, not cosmetic. Fixed on mobile with shared `openReviewModal`/`closeReviewModal` helpers that always reset; fixed on web with a reset-on-open effect.
- **P2 — mobile's manual post-confirm chat refresh raced the 15s background poll (`94951c8`)**: `handleConfirmDelivered`/`handleConfirmPickedUp` called `setChats` completely outside the poll's own staleness guard, so a poll already in flight when a trade step was confirmed could resolve afterward with the pre-confirmation list and silently revert `tradeStatus`, making the just-confirmed button reappear. Fixed by lifting the guard to a ref shared by both the poll and these two call sites.
- **P2 — mobile chat/message deletion persisted to AsyncStorage as fire-and-forget (`94951c8`)**, no error surfaced on a write failure — a "deleted" chat/message could silently reappear on next app launch with zero indication the delete hadn't stuck. Now awaited with an Alert on failure.
- **Verified clean**: mobile's trade-completion stepper's own await/catch/in-flight-guard/error-Alert shape, the message-send/offline-queue path, and the chat-list/message polling staleness guards were all already correct — this audit deliberately scoped around those (already verified earlier this session) to focus on the unaudited parts of the same file.
- Also spot-checked (clean, no changes) five smaller mobile settings screens (`SellingBuyingSettingsScreen`, `NotificationSettingsScreen`, `ProfileStoreSettingsScreen`, `SearchScreen`, `DiscoverSellersScreen`) — all already correctly implement optimistic-update-with-rollback, proper error surfacing, and (where relevant) request staleness guards.

`tsc --noEmit` and `npm run build`/mobile `tsc --noEmit` clean after every commit this round.

**Follow-up — final sweep for the WhatsApp-link-validation gap, both platforms (`3c572a6`).** After fixing this pattern 4x across the session (web `SellerProfilePage.tsx`/`ProductDetail.tsx`, mobile `SellerProfileScreen.tsx`, spotted while reviewing `ChatsScreen.tsx`), grepped every `wa.me/` reference in both `src/` and `mobile/src/` to confirm completeness. Found three more real instances: web `ChatInterface.tsx`'s in-chat-header WhatsApp button was the worst of all of them — it did *zero* country-code normalization (just stripped non-digits), so since nothing on save normalizes a phone number to international format, the common case (a number stored in local Ghana format like "0244123456") built a link WhatsApp can't resolve at all — likely broken for most users' actual stored format, not a rare edge case. Hidden entirely now when the number doesn't normalize to a valid one (this button has no confirmation-step to show an error through, unlike the other fixes). Mobile `HomeScreen.tsx`'s video-feed contact option and `ProductDetailScreen.tsx`'s WhatsApp button both had normalization but no validation — same explicit-Alert fix as the others. The remaining `wa.me` references in the codebase are confirmed to be hardcoded TedBuy support numbers, not user data.

---

**✅ Eighth autonomous session — account deletion's fake password check (both platforms), FollowersFollowingScreen, 2026-09-17.** Continuing the same unattended stretch. Two background audits (mobile `AccountSecuritySettingsScreen.tsx`, `FollowersFollowingScreen.tsx`) surfaced the most consequential finding of this whole session on the app's single most irreversible action.

- **P0 — web's `deleteAccount` had a completely fake password check (`d668197`).** `deleteAccount(password?: string)`'s own type signature, `AccountSecuritySettingsTab.tsx` (already collecting and passing `deletePasswordText`), and `EmailAuthProvider`/`reauthenticateWithCredential` (already imported into the exact same file) all implied a real re-authentication step existed — but the implementation never read the `password` argument at all. The UI's only actual check was `deletePasswordText.length < 6`, which any six-character string satisfies regardless of whether it's the account's real password. The "enter your password to confirm" step visibly presented as a safeguard (e.g. against a shared/unlocked device) while providing zero protection. Wired in the already-imported `reauthenticateWithCredential` for non-Google accounts — completing a feature whose pieces were already in place but never connected, not building new capability.
- **P0 — the same call also swallowed every deletion failure and showed false success (`d668197`)**, same defect shape as `adminToggleSecurityHold` fixed earlier this session: `!res.ok && !data.success` (both required) never detected a `{success:false}` response as a failure, and the whole thing sat in a try/catch that only warned, never rethrew. Every failure mode fell through to local cleanup running unconditionally — products archived, local data wiped, user signed out, told "closed and anonymized" — while the real account and data sat untouched server-side. Also fixed a silent `if (!currentUser) return` that resolved as success with no server call at all if the session had already expired.
- **P1 — mobile's equivalent `deleteAccount()` had the same silent-null-user false-success bug, plus discarded the server's real outcome message (`8f235c9`)**: a security-hold account is frozen/queued for compliance review (nothing anonymized) rather than the normal soft-delete path, but the client always showed the same hardcoded "closed and anonymized" text regardless. Fixed to return and surface the server's real message. **Deferred, documented not fixed**: mobile's delete-account screen has no password-re-entry UI at all (only typing "DELETE") — closing this to match web's fix means designing new UI, not a contained logic change, so it wasn't done blind during unattended time.
- **P1 — `FollowersFollowingScreen.tsx`'s own Following list didn't reflect its own follow/unfollow actions (`f2b6df6`)**: computed from a one-shot `targetProfile` fetch that the toggle handler never updated (only a separate `currentUserProfile` got the optimistic update) — unfollowing someone left the row and count stuck until the screen was left and re-entered. Fixed by preferring the live profile copy when viewing your own list. Also fixed a shared (not per-row) in-flight guard that silently dropped taps on other rows, and preemptively added the same "reset state on target change" guard already applied to `SellerProfileScreen.tsx`/`SellerProfilePage.tsx` this session, ahead of this screen's own stated plan to support arbitrary target users next.
- **P2 — `handleSignOut` had no error handling on both mobile screens that have it** (`ProfileScreen.tsx` fixed last round, `AccountSecuritySettingsScreen.tsx` fixed this round) — an unhandled rejection left the user still signed in with the dialog already closed and no explanation.

`tsc --noEmit` and `npm run build`/mobile `tsc --noEmit` clean after every commit this round.

---

**✅ Seventh autonomous session — mobile's remaining high-traffic screens (ProfileScreen, SellerProfileScreen, ProductDetailScreen), 2026-09-17.** Continuing the same unattended stretch, now pivoting from web (which had a very thorough sweep this session) to mobile screens that had accessibility passes earlier but no dedicated correctness audit. Three background audits ran in parallel, cross-referenced against `mobile/src/firebase.ts`.

- **P1 — `updateUserProfile` (mobile) permanently locked out profile edits for any account with a legacy over-length username (`56da108`).** Its username length/reserved-name checks ran unconditionally, falling back to the already-stored username whenever a call omitted it — so a username predating the 50-char cap blocked every future profile-only edit (avatar, bio, phone, notification prefs) with a misleading "Store Name must be 50 characters or less" error unrelated to what was actually being saved. Fixed to only validate username when a call actually changes it; added `maxLength={50}` to the registration input to prevent new instances.
- **P1 — mobile `updateProduct` silently returned `undefined` instead of throwing on a load failure (`56da108`)**, indistinguishable from success to its callers. `ProfileScreen.tsx`'s Mark Sold handler substituted optimistic values whenever the result was falsy, and `ProductCard.tsx`'s equivalent unconditionally showed a "Listing Sold!" success alert — both treated a failed update as confirmed. Now throws a real error.
- **P1 — `SellerProfileScreen.tsx` showed stale seller data across in-place navigation (`e6d811c`).** React Navigation reuses this screen's mounted instance when navigating here from another seller's followers/following list (only `sellerId` param changes) — the fetch effect never reset state first, so switching sellers kept rendering the previous seller's name/avatar/listings/reviews/follow-state until the new fetches resolved, and if the new seller didn't exist, the not-found screen never rendered at all (stale `seller` was still truthy). Fixed with a ref tracking the last genuinely-fetched sellerId, resetting state only on a real change — also closed the adjacent bug where the follow-toggle in-flight guard was instance-scoped rather than seller-scoped.
- **P2 — mobile's `SellerProfileScreen.tsx`/`ProductDetailScreen.tsx` fixes**: a malformed WhatsApp number produced a dead link with no explanation (same fix as web's equivalent this session); a "quick-switch" between a seller's other listings updated the displayed product without updating the `productId` prop other handlers (Save/Report/Chat) still read from, so those actions could silently target the wrong listing after switching — fixed by reading `product.id` instead of the stale prop, matching what mark-sold/delete already did correctly.
- **P2 — `handleSignOut` (mobile) had no error handling around `logOut()` (`56da108`)**, an unhandled rejection on failure with the dialog already closed; the focus-triggered listings refresh discarded a `failed` flag, so a transient fetch error wiped "My Classified Listings"/"Saved Bookmarks" to real-looking empty states.
- **Verified clean, no changes needed**: `ProductDetailScreen.tsx`'s save/follow/mark-sold/delete/report handling, its main product-fetch race guard, and view-count dedupe were all already correctly implemented — no Boost UI exists on that screen at all.

`tsc --noEmit` clean after every commit this round (same pre-existing, unrelated `HomeScreen.tsx` errors confirmed present before this session, never touched).

---

**✅ Sixth autonomous session — remaining high-traffic surfaces (SellerProfilePage, ProductDetail, VideoAdsFeed), 2026-09-17.** Continuing the same unattended stretch. Three background audits ran (two in parallel, then VideoAdsFeed once those returned) covering the seller storefront page, the single-listing detail page, and the TikTok-style video feed — the last major untouched pieces of the app's core browsing/engagement surface. A clear repeated pattern across all three: Save/Follow/chat-start buttons calling context functions that throw on failure, without await+catch, so a transient error is an unhandled rejection with zero user feedback — the same bug class fixed earlier this round in ProfileSettings.tsx and Navbar.tsx, just not yet swept across every component that has it.

- **P1 — `updateUserProfile` had no rollback-on-failure (`6625817`)**, same gap class as `updateProduct` (fixed earlier this round). Optimistically applies the new profile before persisting, and on failure correctly rethrows but never reverted first — SellerProfilePage's avatar upload showed a new photo as saved even when nothing persisted, silently reverting on next refresh with no explanation. Fixed with the same snapshot/rollback pattern at the source in AppContext.tsx, benefiting every caller.
- **P1 — Save/Follow/chat-start buttons fired without await+catch, swept across three files (`6625817`, `efa1b78`, `5813f1b`)**: SellerProfilePage's main Follow button, its Following/Followers modal's per-row Unfollow/Follow/Follow Back buttons, and its "Chat on TedBuy" button; ProductDetail's Save and Follow buttons; VideoAdsFeed's Save, Follow, and both "Contact Seller" chat-start buttons (the latter two additionally had `setCurrentView('chats')` firing unconditionally the instant `startChat` was *called* rather than when it *succeeded* — the same "reported success with nothing saved" shape as the registration fix from earlier this round). All now await, catch, toast on failure, and (where repeatable) have an in-flight guard against double-submit.
- **P2 — the same raw-JSON-error-blob leak swept across the same files**: `handleToggleSold`/`handleReportSubmit`/`handleMessageSeller`/`handleStartChatInline` in ProductDetail.tsx, plus the newly-added catches above — all now use the established `JSON.parse(err.message).error` unwrap.
- **P2 — SellerProfilePage's WhatsApp contact link had no format validation (`6625817`)**: a malformed number on a seller's profile silently produced a broken `wa.me` link, landing the user on WhatsApp's own "invalid number" page with zero explanation it was the seller's data at fault. Added a format check with a clear in-app error instead.
- **Deferred, documented not fixed**: VideoAdsFeed can mount up to 19 real `<video>` elements at once (an intentional instant-scroll-back cache window) — flagged as a possible iOS Safari concurrent-decoder ceiling risk on long scroll sessions, distinct from what the existing code comment addresses. A real behavior change with no local test coverage; not safe to do blind during unattended time.
- **P3 cleanup, same commits**: removed a dead hardcoded-per-seller-id follower-count variable in SellerProfilePage.tsx that nothing read.

`tsc --noEmit` and `npm run build` clean after every commit this round.

---

**✅ Fifth autonomous session — auth/admin/profile audit (ProfileSettings, AdminUserManagement, Navbar auth flows), 2026-09-17.** Continuing the same unattended "patrol every aspect" stretch, following up on the BoostModal mount-pattern fix. Three background audits ran (two in parallel, then Navbar.tsx once those returned) over the app's remaining major untouched surfaces: account settings, the admin user-management panel, and the login/register/OTP flow.

- **P0 — `adminToggleSecurityHold` could report a fake success on failure (`c21ce4f`).** Its failure check was `!res.ok && !data.success` (both required), so a 200 OK carrying `{success:false, error:...}` was never even detected as an error, and the whole check sat in a try/catch that only `console.warn`'d, never rethrew. Every failure mode fell through to unconditionally marking the hold as applied locally and toasting "Security hold placed" success — on a fraud/security control an admin relies on actually working. Fixed: `||` check, no more swallow, local state/toast only after a confirmed success.
- **P2 — Admin audit-log/deleted-accounts fetch failures were invisible (`c21ce4f`).** Both only `console.error`'d, leaving their lists at `[]` — indistinguishable from a genuinely empty result in exactly the security-review context where that distinction matters. Added distinct error panels.
- **P1 — Admin "delete user profile" was completely unreachable from the UI (`984e1c7`).** The confirmation dialog, state, and `adminDeleteUserProfile` call were fully implemented and correctly guarded (the server independently re-verifies admin status), but nothing anywhere in the app ever opened that dialog — the moderation list only rendered Suspend/Reactivate and Send Email per row. Added a Delete button; removed a second piece of dead state (`confirmDeleteId`) for the same orphaned feature.
- **P2 — Follow/Unfollow from the Following/Followers modal failed completely silently (`984e1c7`)**, catching real thrown failures with only `console.error` — no toast, no state change, nothing to tell the user their click did anything. Added error toasts; also unwrapped the raw JSON error blob already leaking through on `handleToggleSold`/`handleRemoveBookmark`'s existing catches.
- **P1 — Registration could report full success with no server profile ever saved (`0166225`).** `registerUser`'s real (non-sandbox) persistence branch caught its own `/api/users/sync` failure and only warned — the function returned successfully regardless, so the UI showed "Account registered and verified successfully!" while no Supabase row existed. Now rethrows on sync failure so it's a real, visible error. Noted but not fully closed: `createUserWithEmailAndPassword` has already succeeded by that point, so a real orphaned Firebase Auth account (no server profile) can still result if sync keeps failing — a complete fix (rollback the auth account, or self-heal on next login) is still open.
- **Three smaller Navbar.tsx auth-modal bugs, same commit**: the OTP-expired countdown re-fired its error toast every second forever once expired (dismissing it undid itself within ~1s) — fixed to fire once on the real transition; the modal's close effect never reset `authMode`/password-reset state, so three call sites that don't explicitly set `authMode('login')` on open (SellerDashboard, SellersDiscoveryView, ChatInterface sign-in prompts) could reopen the modal stuck on Register or the reset-sent screen — fixed centrally on close; "Continue with Google" wasn't gated by the regular submit's in-flight state, allowing two concurrent auth flows from one modal.
- **Two P3s, same commit**: Resend Code had no double-submit guard and sanitized the typed email differently than the initial submit (risking a desync between the pending-registration email and what verification later compares against).
- **Verified clean, no changes needed**: mobile's `ChatsScreen.tsx` send-message handling already has full parity with (and predates) the web ChatInterface fixes from the prior round — await/catch, offline queue, per-send guard, `maxLength=5000` — no gap found. `ResetPasswordModal.tsx` and `SuspendedBlockModal.tsx` read clean on a direct pass (proper await/catch, no premature close, no double-submit risk).

`tsc --noEmit` and `npm run build` clean after every commit this round.

---

**✅ Fourth autonomous session — chat/review/seller-dashboard audit (web ChatInterface, ReviewModal, SellerDashboard, AppContext), 2026-09-17.** Continuing the same unattended "patrol every aspect" stretch. Two background audits (ChatInterface.tsx, SellerDashboard.tsx) ran in parallel over the messaging and seller-management flows, which hadn't had a dedicated pass this session — all findings traced to exact line numbers and verified against the real `sendMessage`/`updateProduct` implementations before fixing.

- **P1 — `handleSend` fired-and-forgot `sendMessage`, no await/catch at all.** `sendMessage` only ever throws for validation-level failures that happen *before* the optimistic bubble is added (client rate limit, empty text, >5000 chars) — a real network failure never throws, it's queued for offline retry instead. Because handleSend didn't await it, a validation throw became a silent unhandled rejection: input already cleared, message never sent, zero explanation. Now awaited, restores the typed draft and toasts on failure, guards against double-submit (`isSendingMessage`, disables input/button), and `maxLength={5000}` added to the composer so the overlong-text case can't be reached by typing at all (`9fc03ba`).
- **P2 — `markAsDelivered`/`markAsPickedUp` catches showed the raw `err.message`**, which for a `handleBackendError`-wrapped failure is a JSON blob containing the user's own auth info (uid/email/etc.) — added the same `JSON.parse(err.message)` unwrapping already established in `ListingModal.tsx`/`ProductDetail.tsx` (`9fc03ba`).
- **P1 — `ReviewModal.tsx` called `onSubmit` fire-and-forget then immediately cleared its fields and closed, regardless of outcome.** A failed review submission silently discarded the user's rating/comment — only a toast, with the modal already gone, explained why (and if they hadn't read it fast enough, no way to tell what happened at all). `onSubmit` is now awaited; the modal only resets/closes on success, shows the error inline, and stays open with the draft intact on failure. Added an `isSubmitting` guard against double-submit. `ChatInterface`'s `onSubmit` callback no longer swallows the error itself (that suppressed the throw ReviewModal now needs to see) — shows a success toast instead, matching the new inline-error handling on failure (`9fc03ba`).
- **P1 — `updateProduct` (`AppContext.tsx`) had no rollback-on-failure**, unlike `deleteProduct`'s existing pattern. It optimistically mutates `products` state (twice, in the full-update branch) before syncing to the server, and `handleBackendError` logs-and-rethrows on failure but nothing ever reverted the mutation first. Concretely: `SellerDashboard.tsx`'s Mark Sold/Available toggle showed the new status as saved even when the server rejected the write. Fixed by snapshotting pre-mutation state up front and restoring it in the catch block, mirroring `deleteProduct` exactly (`5ea10fa`).
- **P2 — `handleToggleSold`'s catch showed the raw JSON error blob** — same unwrapping fix applied (`5ea10fa`).
- **P1 — `SellerDashboard.tsx` never consumed `productsLoadError`/`isProductsLoading` from context**, so a genuinely-failed listings fetch rendered the exact same "No products listed" empty state as a real zero-listings seller, no way to tell the difference or retry. Added distinct error (red panel, "Try Again" wired to the already-existing `retryLoadProducts`) and loading states ahead of the real empty state (`5ea10fa`).
- **Deferred, documented rather than fixed**: the deeper architectural issue behind "a failed send silently vanishes from the thread within ~4 seconds" — `sendMessage`'s catch *always* resolves successfully via the offline-retry queue and never rethrows for a genuine (non-validation) failure, and the open-chat poll replaces the entire `messages` array from the server's list every 4s, so an optimistic bubble for a message that will never actually send just disappears with the next poll tick, no different in appearance from one that's merely delayed. A real fix needs message-status states (sending/sent/failed/queued) rendered in the UI — judged too large to do blind in an unattended stretch; the awaited-send fix above narrows this to only the true "will never succeed" cases, but doesn't close it. `BoostModal`'s unconditional-mount pattern in `SellerDashboard.tsx` (same shape as `ListingModal`'s pre-fix Cancel/Back bug) was flagged in the prior audit for a dedicated follow-up and still hasn't had one — not confirmed as a live bug, just unverified. `isSystemAlert`'s emoji-prefix check (`msg.text.startsWith('📦')`/`'🤝'`) can misclassify a genuine user message that happens to start the same way — low severity, not fixed.

`tsc --noEmit` and `npm run build` clean after every commit this round.

**Follow-up — final sweep for the same fire-and-forget pattern, web-wide (`dfd858f`).** After fixing this bug class in SellerProfilePage/ProductDetail/VideoAdsFeed/ProfileSettings/Navbar/ChatInterface/AdminUserManagement, grepped the rest of `src/` for any remaining un-awaited `toggleSaveProduct`/`followSeller`/`unfollowSeller`/`startChat` calls to confirm completeness rather than assume it. Found two stragglers: `ProductCard.tsx:66` (`handleSaveClick` — the single most-rendered component in the whole app, every listing card everywhere) and `SellerDashboard.tsx:672` (the Saved-tab watchlist's own separate inline unsave button). Both fixed the same way. Re-grepped after fixing — clean. One remaining un-awaited `startChat` (`ProductDetail.tsx:977`) confirmed to be genuinely dead code (its only trigger, `setSafetyTipsPendingAction('message')`, is never called anywhere in the file) and left alone.

**Mobile spot-checked for the same pattern — already clean, no changes needed.** `mobile/src/context/SavedProducts.tsx`'s `toggleSaved` already does correct optimistic-update-with-rollback-and-rethrow, `ProductCard.tsx` and `HomeScreen.tsx`'s callers already properly await/catch/Alert. A repo-wide grep for the equivalent un-awaited pattern (`toggleSaved`/`followSeller`/`startChat`/`sendMessageApi`/`markAsDelivered`/`markAsPickedUp` called without `await`) across all of `mobile/src` came back empty — consistent with mobile's dedicated reliability audit earlier in this same overall session already having closed this class of bug there.

**Follow-up — `BoostModal.tsx`'s unconditional-mount pattern, resolved (`90612e1`).** Confirmed live: `SellerDashboard.tsx` reuses one `BoostModal` instance across every product, so it never actually unmounts between boost sessions. Two real bugs from that: (1) card number/expiry/CVV and the last-picked payment method weren't reset on reopen — boosting product A, typing card details, closing without paying, then boosting product B showed A's card details still filled in; (2) the MoMo countdown simulator and the card demo path's 1800ms delayed auto-verify are plain timers in component state with no unmount to cancel them — closing the modal mid-countdown didn't stop it, so a boost could silently verify/activate in the background seconds after the user believed they'd canceled. Fixed: full form reset (now including payment method) on every open; on close, checkoutStep resets away from `'momo-push'` (letting that effect's own cleanup cancel its pending tick) and the card-demo timeout is explicitly cancelled via a ref.

---

**✅ Third autonomous session — dedicated listing-creation/edit audit (mobile SellScreen + web ListingModal), 2026-09-17.** Vincent stepped away for ~6 hours ("patrol/inspect the code and try to improve every aspect"). Two background audits ran in parallel on the one major flow that hadn't had a dedicated pass all session: mobile's `SellScreen.tsx` (the listing wizard) and web's `ListingModal.tsx` (its direct counterpart) — both genuinely high-stakes, both flagged in earlier passes as "too complex/risky to touch blindly." That caution turned out justified: real P0/P1 data-loss and silent-failure bugs on both platforms, all found via full-file reads and verified against the actual server-side validation before fixing, not inferred.

**Mobile (`SellScreen.tsx`), worst first:**
- **P0 — edit-mode's full-record backfill fetch failure was silently swallowed.** `editProduct` (as passed from a listings grid) is only a feed-summary object — no `description`, one photo instead of the full array. A second fetch is supposed to backfill both; its failure (`.catch(() => {})`) left the form showing the truncated optimistic seed with zero indication, and hitting Save in that state overwrote the listing's real description/photos while showing a cheerful "Ad Updated" success alert. Fixed: `editDataLoadState` tracks the fetch, `handlePublish` now refuses to save until it's `'ready'`, with a Retry action on failure (`71ec19e`).
- **P1 — a removed video could resurrect itself.** Unlike images (scoped by id via `updateImage`, so removing one mid-upload makes its own completion a safe no-op), video upload completion applied unconditionally — `handleRemoveVideo` just did `setVideo(null)` with no cancellation, so an in-flight upload finishing moments later silently repopulated the video slot with the exact video just discarded (potentially into a fresh draft that had already moved on). Fixed with a generation-counter guard (`b5a5100`).
- **P1 — a successful edit save never reset the form**, only `setEditProduct(null)` — every other field stayed exactly as it was, so posting a genuinely new listing right after editing one could start silently pre-filled (or worse, pre-photographed) with the just-edited listing's stale data. Hoisted `resetForm()` out of create-mode's local scope so both success paths share it (`93c730f`).
- **P2 — edit-mode's camera had no mic-permission check**, unlike the identical action in the new listing wizard (`4528869`).
- **Deferred, documented rather than fixed**: the 18MB video-size cap is enforced for library-picked videos but not in-app camera recordings — closing it properly needs `expo-file-system` (not currently a dependency) to check the recorded file's size, and installing a new package unattended, with no way to verify it doesn't disrupt the live dev session or EAS build config, was judged too risky to do blind. Also noted, not fixed: edit-mode's permission-denied Alert lacks the wizard's "Open Settings" button; edit-mode video recording always stores the max duration instead of the actual recorded length; removing an image mid-upload doesn't cancel the request (minor orphaned-asset storage cost only, the image state itself is already safely id-scoped).

**Web (`ListingModal.tsx`):**
- **Same P0 pattern independently found by direct review** (the dedicated audit for this file didn't catch it, but reading the seeding effect while fixing something else did): the edit-mode backfill fetch had the identical swallowed-catch bug. Same fix shape as mobile — `editDataLoadState`, blocks Save until ready, retry on failure (`b915b74`).
- **P1 — Cancel/Back weren't guarded against an in-flight submit at all.** Clicking Cancel while uploads/`createProduct` were still running let that work keep going invisibly — `SellerDashboard.tsx`'s mount site doesn't even unmount the modal on close, so on success it silently auto-navigated to the "canceled" listing's detail page once the request resolved. Now a no-op (and visually disabled) while `isSubmitting`, matching the Submit button's own guard (`b915b74`).
- **P1 — the unsaved-changes confirmation never fired in edit mode at all** (`hasData && !productToEdit` structurally excluded it — editing means every field starts non-empty, so the raw content-check was never a useful signal there). Replaced with a real dirty-check: a snapshot taken right after seeding from the fetched listing, compared against current values at Cancel/Back time. Also closed a related gap where a video sitting in the trim editor (picked but not yet confirmed) was invisible to create-mode's own check (`b915b74`).
- **P2 — title/description validation was looser than what `createProduct` actually enforces** (150/5000 chars, no minimum, vs. the real 5-100/10-3000), and edit mode had no length validation at all — a too-short/long title or description could sail through a full photo/video upload before being rejected afterward. Tightened to match, applied to both modes (`b915b74`).
- **P2 — direct video upload had no retry**, unlike images' 3x-with-backoff (`5521e93`).
- **P2 — Cloudinary's silent base64-data-URL fallback** (fires after every real upload retry fails) was completely invisible — indistinguishable from a real success, so a listing could quietly publish with a multi-megabyte string embedded instead of a CDN link. Added `isFallback` to the upload result; the post-publish/save toast now tells the user which media needs re-uploading instead of claiming plain success (`e5bda88`).
- **Deferred, documented rather than fixed**: no per-item upload status/retry UI on web (mobile's `SellScreen` tracks per-photo/video status with individual retry; web re-uploads everything from scratch on any single failure) — a real parity gap, but a UI rebuild, not a contained fix. Also noted: a rapid double file-select can exceed the stated 10-photo cap by a couple of images (stale-closure race in the async compression loop) — minor, cosmetic-cap-only.

**✅ Second autonomous session, continued still further — mobile gestures, chat photos, and the online-presence saga, 2026-09-17.** Live, interactive work directly with Vincent (not autonomous — he was testing on a real device via a local Expo dev server this whole stretch), so this entry is denser than usual: real bugs found via live testing, several real dead-ends chased and corrected in front of him rather than quietly.

**Mobile swipe-back gesture, unified**: `ChatsScreen`'s inbox↔thread swipe-right-to-go-back already existed but as a bespoke `PanResponder` with its own dx/dy heuristic. Replaced with the same `react-native-gesture-handler` `Gesture.Pan()` pattern already proven on `HomeScreen`'s grid/video swipe and `ProfileScreen`'s settings/dashboard swipe (composes correctly with a nested FlatList's own scroll recognizer, which the old heuristic didn't do as reliably) — extracted into `mobile/src/hooks/useSwipeBack.ts` for reuse. `SellScreen`'s multi-step listing wizard is a similar drill-down candidate but deliberately excluded — live camera view + an in-progress unsaved draft, too risky to add a swipe there without device testing (`ec5d22e`).

**Chat list avatars always fell back to initials, never a real photo (`afd675e`)**: `/api/chats/start` sets `buyerName`/`sellerName` on a new chat row but never `buyerPhoto`/`sellerPhoto` at all. Fixed at the shared read endpoint (`GET /api/chats`, used by both web and mobile) rather than the write path — on every list fetch, batch-look up each chat's buyer/seller CURRENT `photoUrl` and overlay it over whatever's stored. Fixes every existing chat immediately (no backfill needed) and stays fresh going forward instead of freezing at chat-creation time.

**Online presence (WhatsApp-style) — built, then extensively debugged live, root cause found, feature simplified per Vincent's direction:**
- Built from scratch: `POST /api/users/heartbeat` (updates the caller's own `lastSeen`), `computeIsOnline()` deriving "online" from recency rather than a stored boolean (a stored flag would get stuck "true" forever once a client stops polling), wired into `/api/users/list`, `/api/users/get`, and `getSellersSummaryData()`. Web's own heartbeat effect turned out to be a pre-existing no-op (only ever updated local React state, never called the server — same RLS-migration-era gap already documented elsewhere in this file) — fixed alongside (`6c20098`, `4f2543c`).
- **Real bug found and fixed**: the heartbeat could get silently recorded under an *impersonated* account instead of the real admin's own, because web's shared `getAuthHeader()` auto-attaches an `x-impersonation-session-id` header whenever any impersonation session is active in localStorage (even one long forgotten), and `verifyUser()` honors it. Fixed on both ends — client strips the header for this one call, server no longer accepts impersonation on this endpoint at all (`e5e4501`).
- Threshold tuned to 90s (from an initial 3min) with the heartbeat/poll intervals shortened to match, per explicit request (`8d91cf1`).
- **The actual, much longer dead-end**: extensive live testing kept showing a green dot on every seller card regardless of real status. Chased through: re-verifying `/api/users/list` directly against production repeatedly (always correct), a full Metro bundler cache-clear + dev-server restart (a real stale-cache issue, but not *the* issue), in-app diagnostic logging (proved the fetched data was correct — only one account genuinely online), and only fully resolved by process of elimination — stripping page elements one at a time down to nothing, which revealed the true cause: **SellerCard's pre-existing "verified" checkmark badge** (a ~15px green-filled circle) was being visually misread as the online dot the whole time, since the two sat close together and both read as "a green circle" at phone-screenshot scale. The online-presence data and logic had been correct through the entire investigation. Recolored the badge blue to prove the diagnosis, then — per explicit final direction — removed the verified badge from `SellerCard` entirely (it wasn't serving a purpose) rather than keep both signals. Final state: `SellerCard`/`SellerProfileScreen` show only a green `onlineDot`, bottom-right of the avatar, gated on real `seller.isOnline` (`17b4f05` through `b9327dc`, several commits reflecting the live back-and-forth including one accidental revert corrected in the next commit).
- **Depends on a schema change not yet applied**: `.ai/handoffs/PRESENCE_SCHEMA_PROPOSAL.md` has the exact SQL (`users.lastSeen`, one nullable timestamp column). Confirmed live via Render logs mid-session that this hasn't run yet (`Could not find the 'lastSeen' column of 'users' in the schema cache`) — every heartbeat write is currently failing gracefully (caught, logged, client swallows it), and every read path already falls back correctly, so nobody shows online in production yet regardless of activity. Once this runs, the feature is otherwise fully wired and correct.
- Popular Stores (`HomeScreen`'s carousel + `DiscoverSellersScreen`'s full list, both via `computeDiscoverSellers`) also redefined per explicit request this same stretch: ranked by most-recent-post instead of accumulated volume/views, capped at 24 on the home carousel, sellers-count badge removed from the section heading (`8591fc8`).
- Web's Discover Sellers header icon recolored from a blue gradient to dark navy (`#0f172a`) to match mobile's equivalent icon, per explicit request (`0e92fe1`).

**✅ Second autonomous session, continued further still — systematic auth sweep of every server.ts route, 2026-09-17.** Prompted directly by Vincent ("keep working, try again") after the moderation-visibility fix below. Rather than keep guessing at what to check next, ran a script against every `app.get/post/put/delete(...)` route in `server.ts` to find every one with no `verifyUser()`/`verifyAdmin()` call anywhere in its body, then manually reviewed each hit. Most were correctly, intentionally public (product browse/search/featured/trending endpoints, pre-auth flows like registration OTP and password reset, the sitemap/robots.txt/health-check/download routes, and the already-audited PII-safe `/api/users/list`/`/api/reviews`) — but found and fixed two genuine gaps:

- **`/api/admin/impersonate/verify` and `/api/admin/impersonate/exit` had zero authentication** (`b23a709`), unlike every other admin-impersonation route (`/start`, `/logs`, both `verifyAdmin()`-gated). `/verify` returned the full session record (admin email, target user id/email) to anyone holding a `sessionId`; `/exit` fetched the caller's verified identity via `verifyUser()` and then never actually checked it, silently accepting any request. Low severity in practice — `sessionId` is a 128-bit `crypto.randomBytes` token, not guessable — but `AppContext.tsx`'s `exitImpersonation()` already sends a real Firebase auth header on this call, so gating cost nothing: the client already behaved as if this were checked, the server just wasn't checking it. Verified live against a local server: both now 403 with no auth header.
- **`/api/sitemap/clear` had zero authentication** (`2e3590a`) despite its 4 real callers (`AppContext.tsx`, after a create/update/delete product call succeeds) already sending a real auth header — same shape as the impersonation gap, fixed the same way (now requires any signed-in user). **`/api/cache/clear`, a separate unauthenticated endpoint, was removed entirely** — confirmed via repo-wide grep it has zero callers in `src/` or `mobile/src/`, and despite its name it only ever cleared the sitemap cache (not the real product-list cache — that's the already admin-gated `/api/products/invalidate-cache`), so it was dead, misleadingly-named surface with no legitimate purpose. Verified live: `/api/sitemap/clear` now 401s with no auth header, `/api/cache/clear` now 404s.

**Checked and deliberately left alone**: the server-side `/api/auth/verify-admin-pin` endpoint is confirmed dead code (the client-side `verifyAdminPIN` in `AppContext.tsx` never calls it — it checks the PIN entirely client-side against `import.meta.env.VITE_ADMIN_PIN || '2330'`, a value that's baked into the public JS bundle regardless of what it's set to). That client-side design is already explicitly documented in its own comment as intentional, low-stakes UX friction, not a real security boundary ("no privileged action should ever treat `isAdminSessionVerified` as proof of anything... the actual privileged endpoints independently re-verify admin status via `verifyUser()`'s cryptographic Firebase-token check regardless of this flag") — already correctly triaged by an earlier pass, not something this session re-opened.

**✅ Second autonomous session, continued further still — a real, broadly-reaching moderation bug found by manual review, 2026-09-17 (`95d9bf9`).** Found while reviewing the SEO/sitemap code near where Vincent's editor cursor was, not from an automated audit — worth flagging on its own, separate from the audit-driven work below, because of how far it reaches.

**`normalizeServerProductSummaryRow()` — the one shared filter behind `getProductsListData()`, which backs `/api/products`, `/api/feed`, `/api/featured`, `/api/trending`, and `/api/similar` — excluded soft-deleted and archived products but not `status === 'hidden'`.** Since this single function gates the app's primary product surfaces, an admin's "hide" moderation action had no actual effect on public visibility: a hidden listing stayed fully visible in the home feed, search, featured/trending, and everywhere else derived from this same cached dataset. (A handful of other call sites — video-ads, a couple of feed filters — already did their own redundant inline `'hidden'` check on top of this already-filtered list, so they were fine; this was the one gap that actually mattered, and fixing it here fixes every downstream consumer at once, including ones like `/api/search/suggestions` that had no redundant check of their own.)

Two related gaps in the same family, fixed alongside:
- **The SSR meta-tag injection for `/product/:id`** (`server.ts`'s catch-all route, the code path that actually matters for crawlers/link-preview bots since they don't execute the client-side JS) had no moderation check at all — `if (data)` alone served a full rich preview (title/OG image/JSON-LD structured data) for any row that still exists, including ones an admin took down. A moderated listing's link stayed shareable with a real-looking preview card on WhatsApp/Facebook/Twitter and indexable by Google regardless of the moderation action.
- **`/api/products/:productId`'s own archived/deleted check was missing `'hidden'` too** — a direct link to a hidden listing still resolved successfully (200, full product data) for anonymous visitors, not just crawlers.
- **The sitemap generator (`src/utils/sitemap.ts`) only excluded `'sold'` listings** from `sitemap.xml`, so a moderated-away listing stayed submitted to Google indefinitely.

Verified by tracing every direct `.from('products')` query in `server.ts` (26 call sites) to confirm none of the others are public-browsing/feed paths bypassing this same filter — the rest are single-product lookups by ID for edit/delete/ownership-verification/admin-maintenance jobs, which don't need a visibility filter since they act on a specific known product for its owner or an admin, not public browsing. `tsc --noEmit` and full production build both clean.

**Correction on severity, checked immediately after fixing — the two live-impact fixes and the one defensive one are not the same size of deal.** `status: 'hidden'` is not actually reachable by anything live today: `upsertProductToSupabase`'s own comment (`server.ts` ~3144) says outright "there's no dedicated admin product-moderation endpoint yet," confirmed by grep (nothing in `server.ts` ever writes `status: 'hidden'`; only the self-reinstatement guard and now this fix *read* it). So adding `'hidden'` to `normalizeServerProductSummaryRow()` (the `getProductsListData()` fix) is real, defensive, future-proofing work — that function's `isDeleted`/`archived`/`deleted` cases were *already* correctly handled before this pass; `'hidden'` was the only addition, and it closes a gap that doesn't have a way to bite yet, same category as an earlier session's `isApproved` fix ("harmless now, becomes a real gap the moment a future feature wires moderation logic to it").

**The other two fixes are live and reachable today**, because the account-deletion cascade genuinely does set `status: 'archived', isDeleted: true` on a departing user's listings (`server.ts` ~7864/7882) — and unlike `getProductsListData()`'s gate, the SSR meta-tag injection for `/product/:id` had **no status check at all** (`if (data)` alone) and the sitemap generator only excluded `'sold'`. So before this fix, a deleted account's archived listings were still being fully server-rendered with rich preview meta tags (shareable on WhatsApp/Facebook with a real-looking card) and still submitted to `sitemap.xml` for Google to index — a real, currently-reachable gap, not a future one. The `'hidden'` addition on top of those two is the same defensive future-proofing as above, bundled in since it's the same one-line pattern.

**✅ Second autonomous session, continued further — mobile reliability/tech-debt audit, 2026-09-17.** A fifth background audit ran with the same lens already applied to web (silent failures, unchecked fetches, missing `.catch()`, request-ordering races, dead code) — the one class of audit mobile hadn't had yet (mobile had already had performance and accessibility passes this session). Found 8 real, distinct issues; verified each against the actual code before acting. Fixed 6 as isolated commits, documented 2 as out of scope for a blind fix:

- **5 raw `fetch()` calls with no timeout, unlike everywhere else in `firebase.ts`** (`43e443b`): `fetchProductsWithStatus` (underlies `watchProducts()`, so every product-list screen), `fetchSearchSuggestions`, `fetchVideoAds`, `fetchSellerListingCounts`, `fetchProductsForSeller` — same exact failure mode already found and fixed once before for `fetchProductById` (a stalled connection leaves the caller spinning forever with no way to recover). Routed all 5 through the existing `apiFetch()` helper instead of a bespoke inline fetch.
- **`ChatsScreen`'s open-thread message poll had no request-ordering guard** (`151992b`), unlike the chat-list poll one function above it in the same file (which already has one, with a comment explaining exactly why). Added the identical `requestId` pattern — without it a slow poll tick could revert the thread and make a just-arrived message disappear until the next tick corrected it.
- **`UnreadChatsProvider` (the tab-bar unread badge) could apply a stale account's poll result after a fast sign-out/sign-in switch on the same device** (`50c18c8`) — `fetchChatsApi()` resolves using whichever token was live when *called*, not when it *resolves*. Added a generation counter, same shape as the request-ordering guards above.
- **`BoostModal`'s payment-verification-failure path treated a genuine "not paid" and a real network/timeout failure on our own verification call identically** (`138b116`) — both silently returned to plan-select with zero message. The second case means the app genuinely doesn't know if the user's card/momo was charged; now surfaces a real alert telling them to check the boost status or contact support before paying again, instead of risking a silent double-charge.
- **~115 lines of dead legacy Firestore chat/message code removed** (`ec4d92c`): `fetchChatsForUser`, `watchChats`, `watchMessages`, `startChat`, `sendMessage` — the file's own comment already called these "kept temporarily, unused, as a rollback reference"; confirmed via repo-wide grep that nothing imports any of them any more, then removed along with the Firestore SDK imports (`getDoc`/`getDocs`/`addDoc`/`orderBy`/`updateDoc`) only they used.
- **2 genuine pre-existing `tsc` errors fixed** (`5c14ed2`, found while doing the accessibility pass, not this audit): `Product.sellerId` is optional but was passed into a required-`string` callback in `ProductCard` and an `Array.includes()` call in `HomeScreen`'s following-feed filter — both guarded. Also deleted `data.ts`'s dead `profile` mock export (type no longer matched, nothing imports it).

**Documented, not fixed** — each needs bigger, cross-cutting work rather than a contained patch:
- **Read-modify-write race on `likesCount`/`viewsCount`/sold-status** (`toggleLikeProduct`, `trackProductView`, `updateProduct` in `firebase.ts`) — all fetch the whole product, mutate one field locally, then upsert the entire object back via `/api/products/sync`, which has no field-level atomic increment. Two concurrent likes on the same listing can silently drop one with no error to either user. Real fix is server-side (an atomic increment on `/api/products/sync`, or a dedicated endpoint), not a mobile client patch — and this exact field was already flagged as "client-influenceable, already bounded by ranking algorithm, low severity" in an earlier session's security sweep, so it's a known, accepted tradeoff rather than a fresh discovery.
- **A single root-level `ErrorBoundary` (`App.tsx`) means any screen's render crash wipes the entire app's state**, including React Navigation's stack and any in-progress form (e.g. `SellScreen`'s multi-step listing draft) — "Try Again" remounts everything, not just the screen that crashed. Same class of finding as web's own single-root-ErrorBoundary note elsewhere in this doc; a real fix means per-screen boundaries around the Stack.Navigator's screen renderers, which is multi-component work, not a quick patch.
- Also noted, deliberately not touched: `navigation/index.tsx`'s `ProductDetail`/`SellerProfile`/`FollowersFollowing` routes access `route.params.X` without optional chaining (unlike `FeaturedListings` right next to them) — a latent crash if some future caller (e.g. a push-notification deep link, which doesn't exist yet) ever navigates there without the expected params. Confirmed unreachable by any current call site, so this is speculative hardening for a feature that doesn't exist rather than an active bug — left alone rather than building it out.

**✅ Second autonomous session, continued — mobile accessibility audit + housekeeping, 2026-09-17.** Same 8-hour unattended stretch as below; a fourth background audit (mobile accessibility) returned after the web-side work was already committed. Its top-line finding: TalkBack/VoiceOver support across the entire `mobile/` app is not partially done, it's **entirely absent** — a repo-wide grep for any accessibility prop (`accessible`/`accessibilityLabel`/`accessibilityRole`/`accessibilityState`/`accessibilityValue`) found exactly one hit in the whole codebase (`DismissKeyboardView.tsx`'s `accessible={false}`, itself correct usage). 15 findings, all traced via direct file reads (not taken on the audit's summary alone), fixed across 9 isolated commits: the shared `BackButton` (used on nearly every non-tab screen) got a label; `ProductCard`/`SellerCard` (the app's two most-repeated composite cards) got outer-card grouping plus labels/state on every nested control (bookmark, mark-sold, follow); `BoostModal` (a real paid-transaction flow) got labels on both close buttons and `radio`/`checked` state on its plan and payment pickers — previously a screen-reader user had no non-visual way to confirm which plan they were about to pay for; `ProfileScreen` got labels on its auth-form inputs and the password show/hide toggle; `SellScreen` got labels/roles across every category/condition/region/city chip, the remove-photo/video buttons, and every text field; `HomeScreen`, `ChatsScreen`, `ProductDetailScreen`, and `SellerProfileScreen` (the last not explicitly named in the audit but sharing the identical unlabeled-chip/composite-row pattern, fixed the same way) got their search-clear buttons, view/tab toggles, filter and category chips, photo carousel, and composite list rows (chat threads, followers/following, related listings) labeled. `tsc --noEmit` clean at every commit (same handful of pre-existing, unrelated errors — a `ProductCard.tsx` prop-typing issue, a `data.ts` object-literal issue, two `HomeScreen.tsx` argument-typing issues, and a `viewMode` narrowing quirk inside the toggle's own JSX block — confirmed present before this pass touched the file, not introduced by it). Commits: `a5e7762`, `14f5b24`, `566777e`, `d981ca3`, `22cf53c`, `1c131d3`, `0e6aa98`, `f22c9be`, `981df68`, `0788399`.

**✅ Second autonomous session — web performance, accessibility, SEO, reliability, 2026-09-17.** Vincent stepped away for ~8 hours and asked for a broad improve-everything pass, this time on genuinely new ground (the previous 4-hour session already covered web/mobile silent-failure bugs and mobile performance). Three background audits ran in parallel: web performance (never audited before), web accessibility/SEO (never audited before), and a reliability/technical-debt sweep. 17 commits, all pushed to `main`, `tsc --noEmit` + full production build clean throughout.

**Housekeeping fixes added after the initial 14-commit batch below, same session**: a visually-hidden `<h1>` added to the homepage (`src/App.tsx`) and a skipped `<h2>`→`<h4>` heading level fixed on product detail's seller name (`45866e2`); dead `getMailTransporter()`/`diagnoseSMTPAndVerify()` nodemailer code removed from `server.ts` — zero call sites, all real email already goes through Brevo (`1ac0e03`, plus `npm uninstall nodemailer @types/nodemailer`); a diagnostic-only `logStartupEnvironmentStatus()` added right after `dotenv.config()`, logging presence/absence of the 8 critical/important env vars (Supabase, Firebase, Cloudinary, Brevo, Paystack, Gemini) in a grouped OK/MISSING summary at boot — verified in isolation via a standalone script first since it runs on the critical server-boot path, does not change any exit/error-handling behavior (`177cb88`).

**The single most business-relevant finding: shared seller storefront links were silently broken.** Three compounding bugs meant a seller sharing their store link got a generic "TedBuy Ghana" preview card everywhere it was pasted (WhatsApp, Facebook, Twitter) — never their actual shop name/photo/bio: (1) `ProfileSettings.tsx`'s "Share Store" button built a hash URL (`/#seller/:id`), which browsers never send to the server, so link-preview crawlers always saw the generic site-wide meta tags; (2) even for a real path, `server.ts`'s SSR handler was checking a stale, unused `/store/:id` scheme instead of the app's real `/seller/:id`; (3) even when matched, it never built real per-seller meta tags at all, just a 404 check. Fixed all three — confirmed via reading `AppContext.tsx`'s `parseUrlState` that real `/seller/:id` paths were *already* correctly parsed client-side, so this closes the loop end-to-end (correct preview card AND correct landing page). Also fixed the identical hash-URL bug in `useSEO.ts`'s own client-side canonical-URL logic for product/seller/category/post-ad views, and product JSON-LD structured data that hardcoded `itemCondition: UsedCondition`/`availability: InStock` regardless of the actual listing (both server-side and in this same client-side duplicate).

**Second-most impactful: `handleBackendError`-class systemic issues, this time in rendering/network patterns rather than silent failures.** `ProductCard` (the primary browsing unit — home feed, search, every category/trending/featured view) had no keyboard access at all (no `tabIndex`/`role`/`onKeyDown` on the card or its nested seller-info strip) — fixed with proper `role="button"` + Enter/Space activation. The min/max price filter inputs drove a full re-filter+re-sort+re-rank of the entire product list on every keystroke with no debounce (unlike `searchQuery`, which already had one) — now debounced 300ms. `productSelector.ts` was running full `console.log`/`console.groupCollapsed` diagnostics on every production recompute, unconditionally — real wasted work plus exposing internal ranking-algorithm scores in any user's browser console; removed.

**Everything else fixed, grouped by area:**
- **Accessibility**: 8 auth-flow form labels (registration/login/reset/OTP) had no `htmlFor` linking them to their inputs; the header search box and its clear button had no accessible name beyond a disappearing placeholder; 7 icon-only modal close/delete buttons (BoostModal, ReviewModal, PrivacyPolicyModal, ResetPasswordModal, ListingModal) had no `aria-label`; BoostModal's custom radio-style plan picker was mouse-only, blocking the entire boost-purchase flow for keyboard users — all fixed.
- **Reliability**: two endpoints found with real gaps — `POST /api/auth/delete-account` had no rate limiter at all (now 5/min); `POST /api/products/invalidate-cache` only required being signed in, not an admin, and had no rate limiter either (confirmed zero client-side callers anywhere in `src/`/`mobile/src/` — a manually-triggered admin tool, not a real feature — now admin-gated + rate-limited). `dbAdapter.ts`'s single-document `onSnapshot` listener had no `.catch()` at all (unlike its own collection-query sibling) — a genuine promise rejection left any single-doc subscriber stuck in loading state forever with no error surfaced. Two duplicated request-ordering races fixed (web `SearchSuggestions.tsx` + `AppContext.tsx`'s chat-list poll, and the identical chat-list poll race in mobile's `ChatsScreen.tsx`) — an older, slower response could land after a newer one and silently overwrite it with stale data. FCM push tokens were being logged in full to the console in two places (a device can be targeted with just the token) — truncated.
- **Performance**: added a real `/api/similar` endpoint (closing the last Featured/Trending-class "derives from the wrong paginated array" gap — this one had no existing dedicated endpoint to redirect to, so a new one was built, mirroring the same pattern); Featured Listings' 1.5s auto-swipe carousel kept scrolling (a state update + layout read) even while the browser tab was backgrounded — now skips that while hidden (confirmed via direct read that Trending Listings does *not* share this pattern, despite being suspected to — no auto-swipe interval exists there at all).

**Deliberately investigated and documented, not fixed** — each would need either a bigger, riskier change than this sandbox can safely make blind (no way to test in a live browser), or a product decision:
- **`AppContext.tsx`'s Provider value is a brand-new object literal on every render, never memoized** (confirmed via full file read) — the single highest-impact web performance finding, but a *safe* fix requires first wrapping all ~90 of its functions in `useCallback` with correct dependency arrays; memoizing just the outer object without that would do nothing (the functions would still be new references every render, so the memo would never actually skip a recompute). Too large and risky to attempt blind on a live production app with real users — needs a dedicated pass with real browser testing, not autonomous execution.
- **Search/category browsing fetches up to 1000 products in one request** (`AppContext.tsx`, `/api/products?...&limit=1000`) — investigated the "load more" pagination mechanism this feeds and confirmed reducing this without *also* building real server-side pagination for search results would likely break "load more" for category/search browsing. A genuine fix needs that pagination work first, not just a smaller number.
- **No modal in the codebase manages focus** (moving focus in on open, trapping Tab, returning focus on close on dismiss) — touches BoostModal, ListingModal, ReviewModal, ResetPasswordModal, PrivacyPolicyModal, and Navbar's auth modal block. A real, contained fix (a small shared focus-trap hook) but risky to apply to 6 components at once without live testing each one.
- Lower-priority items noted but not acted on: a single top-level `ErrorBoundary` (a crash in any modal/route currently takes down the whole app, not just that piece — architectural, not a quick fix); dead SMTP/nodemailer code in `server.ts` (never called, all real sends go through Brevo — safe to delete whenever, just cleanup); a hardcoded `'2330'` admin-PIN fallback (already a non-security-boundary by design, every real privileged endpoint re-verifies independently); ~219 occurrences of low-contrast `text-slate-400` (a real WCAG AA concern given this is a mobile-first, often-outdoor-viewed Ghana marketplace, but a mass color change needs design review, not a blind find-replace); robots.txt/index.html/JSON-LD's own no-`<h1>`-on-homepage and one skipped heading level on product detail.

Full commit list this session (web + housekeeping): `f4f1215`, `5c5dcc0`, `7432ece`, `b3f37b6`, `8f3260b`, `3bd6b04`, `2abc841`, `0b17aec`, `7e246cc`, `88a9268`, `8bc3845`, `64ef9c8`, `6cae179`, `ed3903c`, `45866e2`, `1ac0e03`, `177cb88`. Mobile accessibility follow-up commits listed in the update above.

**✅ Mobile performance pass + mark-as-sold sync bug, 2026-09-16.** Two real Android testers reported the app feeling slow; separately, marking a listing sold needed "refreshing twice" before the UI caught up. Investigated with a dedicated audit (rendering/images/lists/network) plus direct tracing, not guessed at.

**Root causes found and fixed:**
- `ProductCard` (rendered dozens of times per screen) was never memoized — every unrelated re-render (a keystroke, the Featured carousel's 1.5s auto-swipe) rebuilt every mounted card's full element tree. Fixed with `React.memo` + a custom comparator, mirroring the exact pattern already proven for `HomeScreen.tsx`'s `VideoFeedRow`.
- Every product photo in every list requested the seller's original full-resolution upload for a ~180px grid cell. Added a Cloudinary thumbnail transform + switched from React Native's `Image` to `expo-image` (adds real caching) across `ProductCard`, `SellerCard`, and `HomeScreen`'s remaining direct image usages.
- Home's search box fired a full 200-item network request on **every keystroke**, no debounce (`SearchScreen.tsx` already had this right at 150ms; Home's box never got it) — client-side filtering already handles search with zero network cost, so this was pure waste. Debounced to 400ms.
- 10 different screens each independently called `watchProducts()`, each firing its own fresh 200-item fetch with zero sharing — visiting 3-4 tabs re-downloaded/re-parsed the same catalog 3-4 times. Added a shared 45s cache + in-flight request dedup.
- Main grid `FlatList` had zero virtualization tuning (the video feed got careful tuning; the grid didn't). Added `initialNumToRender`/`maxToRenderPerBatch`/`windowSize`, with `removeClippedSubviews` gated to Android only — the codebase's own existing comment documents why that combination caused freezes for the *video* feed specifically (native decoder conflict); the grid is plain images, so it's safe there and matches the platform the actual complaint came from.
- **Mark-as-sold bug**: `ProductCard`'s toggle only wrote to the server — it had no way to tell its parent screen to update local state, so the card kept showing the pre-toggle status until an unrelated refetch happened to catch it (explains "refresh twice"). Added an `onProductUpdated` callback, wired into all 10 of `ProductCard`'s call sites across the app.

6 commits, `tsc --noEmit` clean throughout (same 6 pre-existing, unrelated errors at every checkpoint), all pushed to `main`: `1034521`, `129783d`, `bf23bb2`, `90f6abb`, `5350da1`.

**Still needs a fresh EAS build to actually reach a device** — none of this has shipped yet; the build that was in progress was killed to fold this work in first. Also queued from earlier: the push-notification schema migration (`.ai/handoffs/PUSH_NOTIFICATIONS_SCHEMA_PROPOSAL.md`, awaiting Vincent's approval) should go in the same build cycle.

**✅ RLS MIGRATION FULLY COMPLETE AND VERIFIED IN PRODUCTION, 2026-09-16.** The `BLOCKED_APPROVAL` status below is now historical — closing the loop on the entire saga this document has been tracking. Full detail lives in the dedicated docs (`RLS_PREFLIGHT_AUDIT.md`, `RLS_PHASE4_PROPOSAL.md`, `RLS_PHASE5_VERIFICATION.md`, `RLS_PHASE5_MANUAL_PROCEDURE.md`); summary here for anyone starting from this file:

- **Pre-flight audit** (`RLS_PREFLIGHT_AUDIT.md`, post service_role migration): confirmed every table's client-side path closed, server consistently on `service_role`, mobile has zero Supabase dependency. One open item — `admin_audit_logs` confirmed absent from production — resolved as non-blocking: all six dependent admin endpoints already degrade gracefully with it missing (no error, no blocked action; only consequence is a missing audit trail for those actions). Verdict: READY FOR RLS.
- **Phase 4** (`RLS_PHASE4_PROPOSAL.md`): exact SQL proposed (10 `ENABLE ROW LEVEL SECURITY` statements, zero `CREATE POLICY`, no grant changes, `admin_audit_logs` deliberately excluded since it doesn't exist), reviewed and approved by Vincent, executed by him directly in the Supabase SQL Editor. Confirmed live: all 10 tables show `rls_enabled = true`, zero policies.
- **Phase 5** (`RLS_PHASE5_VERIFICATION.md` + `RLS_PHASE5_MANUAL_PROCEDURE.md`): live-tested against production using the real (publicly-shipped) anon key — all 10 tables reject anonymous reads, 5 representative tables reject anonymous writes with the correct `42501` RLS-violation error, `service_role` connectivity confirmed via 4 independent live endpoints. Three manual tests (authenticated write/read, cross-user authorization, admin action) walked Vincent through step-by-step and all passed — cross-user specifically confirmed live via DevTools (`403 Forbidden`, verified the rejection happens before any DB write). **Final status: PASS.**

**Post-RLS bug hunt, same session** — testing surfaced and fixed several real, unrelated bugs (none RLS-caused; RLS itself was clean throughout):

- **Boost deactivate — two compounding bugs, both fixed** (`84edf42`, `beff67b`): deactivating a boost correctly set `boostStatus: false` but left `lastBoostedAt`/`lastBoostPurchase` stale, which `getBoostEndDate()`'s fallback chain used to reconstruct a still-future end date — so the UI kept showing "Active Boost" indefinitely. First fix cleared those fields in boost-control's response; that alone didn't work because a *second*, deeper bug in `upsertProductToSupabase` used `field || undefined` for those two fields specifically (unlike their neighbors, which correctly used `|| null`) — an explicit `null` became `undefined`, which `JSON.stringify` drops, so the clear silently never reached the database no matter how many times deactivate was clicked. Found via fresh Render logs plus direct Table Editor verification at each step, not guessed at.
- **Featured Listings carousel showing nothing or an incomplete set** (`52b6ffb`): the home-page carousel derived its list from the general, paginated `products` context array (24 items, sorted by recency, not boost status) instead of the dedicated `/api/featured` endpoint (which correctly queries up to 1000 products) — so it could show nothing at all or an incomplete subset depending on what happened to be in that page. Fixed to always source from `/api/featured`, matching the "View all" screen's already-correct pattern.
- **Mobile: Google sign-in silently reusing the previous account** (`9eec009`): `logOut()` only cleared the Firebase session, never the native Google Sign-In SDK's own separate cached session — fixed to clear both.
- **Mobile: no direct Settings-hub logout** (`1112a7c`): added, per Vincent's request.
- **Mobile: "posted a product but it's not in the database"** — traced to Vincent's installed app predating a real fix already in the repo since 2026-09-03 (`b1ffd75`): the old `createProduct()` fired the save request and always returned success locally regardless of server outcome. Current code throws a real error instead. Not a live bug — needs a fresh EAS build + redistribution (Vincent's own action, this sandbox has no EAS credentials or network path to `api.expo.dev`).
- **Perf**: `getProductsListData()` (backs home feed/search/featured/trending) was requesting a confirmed-nonexistent `products.updatedAt` column on every call, guaranteeing a failed first attempt and a silent fallback to a slower `select(*)` every single time (`5c775dc`) — no schema touched, pure query fix.
- **Observability**: `/api/verify-payment`'s replay-guard failure logging couldn't distinguish a genuine duplicate-payment-reference hit (benign) from a real insert failure that leaves a reference unclaimed and replayable later (a real gap) — now logged at different severities so the real case doesn't blend into routine noise (`7e138a5`).

**Autonomous work session complete, 2026-09-16** — Vincent stepped away for ~4 hours and asked for a broad improve-everything pass across both apps. Two background audits ran in parallel (web silent-failure/data-integrity sweep, mobile parity/reliability sweep), each grepping extensively across the two largest, most central files (`server.ts`/`src/context/AppContext.tsx` on web, `mobile/src/firebase.ts` on mobile) plus every screen/component. 17 findings total; every one traced and confirmed via direct code reading before being acted on, none taken on the audit's word alone. 15 fixed as isolated, `tsc`/build-verified commits; 2 deliberately documented instead of built (see below). No RLS/policy/grant/schema changes — none of this touches that system.

**The single most impactful finding: a systemic silent-failure pattern, not an isolated bug.** `handleBackendError()` (`src/firebase.ts`) — the shared error handler behind ~9 of web's core write paths (`createProduct`, `updateProduct`, `markAsDelivered`/`PickedUp`, `followSeller`/`unfollowSeller`, `createReport`, `startChat`) — only ever threw for permission-type errors; every other failure (network error, validation rejection, a 5xx, a timeout) was `console.warn`'d and swallowed regardless of whether the operation was a read or a write. Every one of its callers does `catch (err) { handleBackendError(err, ...); }` with nothing after it, so the calling function resolved successfully even though nothing was saved — e.g. `ListingModal.tsx` could show "Ad updated successfully!" after a genuinely failed save. Fixed at the single shared function (verified every call site's `operationType` tagging first): any non-read operation now always throws on failure, reads keep the original graceful-degradation behavior. Confirmed this activates already-correct, already-existing error-toast logic in `ListingModal.tsx` that had simply been unreachable for non-permission failures until now — not new UI code, just finally wired up.

**`createProduct` had a second, worse bug layered on top**: its actual persist call was a bare, un-awaited `fetch()` with no response check *at all* (`fetch()` only rejects on a network-level failure, never a 4xx/5xx) — meaning any server-side rejection left the optimistically-inserted listing sitting in local state indefinitely while `createProduct` still resolved successfully. **This is the same failure mode already found and fixed in mobile's `createProduct` back on 2026-09-03 (commit `b1ffd75`) — except this one was confirmed live in the current web code, not historical.** `deleteProduct` had the identical shape (unawaited `.then()` chain, response never checked) — all three of its UI callers show "deleted successfully" unconditionally right after `await deleteProduct(...)`. Both fixed: properly awaited, checked against `data.success`, and rolled back (optimistic state reverted) on failure, with the real error now propagating through the `handleBackendError` fix above.

**Second-most impactful: the same "derives from the wrong, paginated data source" bug (already found and fixed once in Featured Listings before this autonomous session) turned out to also affect Trending.** Both `TrendingListings.tsx` (the carousel) and `TrendingListingsView.tsx` ("View all") derived from the general `products` context array (24 items, recency-sorted) instead of the dedicated `/api/trending` endpoint — the "View all" page never called it at all, and had a second, independent bug on top: its sort compared `.views`, a field that doesn't exist on `Product` (only `viewsCount` does), so every comparison was `0 - 0 = 0` and the "sort by popularity" never actually did anything. Both now source from `/api/trending`, matching the fix already applied to Featured. `FeaturedListingsView.tsx` also still fell back to the wrong source on a fetch *error* (a narrower version of the same bug); fixed to keep previous good data instead.

**Everything else fixed, in descending order of severity:**
- **Failed avatar upload silently saved a broken `blob:`/`data:` URL as the permanent profile photo** (`ProfileSettings.tsx`) — a `blob:` URL is only valid in the tab that created it, breaks on reload/everywhere else. Now aborts the save with a clear error instead.
- **`/api/admin/boost-control` always returned `success: true`** regardless of whether the underlying Supabase/Firestore writes actually succeeded — now tracks the real Supabase write outcome (the authoritative store) and returns a genuine error if it failed; Firestore stays best-effort as before, since it's a secondary sync for mobile's realtime listeners.
- **`/api/verify-payment`'s replay-guard failure logging** couldn't distinguish a genuine duplicate-reference hit (benign) from a real insert failure leaving a reference unclaimed and replayable later (a real gap) — now logged at different severities.
- **`getProductsListData()`** (backs the home feed/search/featured/trending) requested a confirmed-nonexistent `products.updatedAt` column, guaranteeing a failed first attempt and a silent fallback to a slower `select(*)` on every single call — pure query fix, no schema touched.

**Mobile fixes, same session:**
- **Offline message queue shared across accounts on one device** (`ChatsScreen.tsx`) — one global, unscoped AsyncStorage key, unlike the correctly-namespaced `tedbuy_deleted_chat_ids_${uid}` right next to it. A message queued offline by User A could get silently replayed under whichever identity was signed in when connectivity returned. Now namespaced per-uid with a one-time migration of anything already queued under the old key, plus each item recording its own `senderId` as a second independent check.
- **Google sign-in silently reusing the previous account after sign-out** (`logOut()` only cleared the Firebase session, never the native Google Sign-In SDK's own separate cached one), plus a **missing direct logout on the Settings hub** (added, per Vincent's request) and a **stale `sellerName` source** (new listings pulled from the Firebase Auth SDK's `displayName` instead of the canonical Supabase profile, which can diverge if a profile-name update's Auth SDK call fails silently while the Supabase write still succeeds).
- **Recently-viewed products and recent searches shared across accounts** on one device (same shared-key pattern as the offline queue) — namespaced per-uid. Deliberately did **not** apply the same change to the per-device view-count cooldown timestamp key — that one is intentionally shared across accounts by design (anti-fraud, matches web), and namespacing it would let someone inflate a product's view count just by switching accounts on the same device.
- **Boost payment could be charged with no boost activated** if the Paystack checkout WebView was closed a moment before the redirect was detected (slow network, closing right after seeing Paystack's own success message) — the reference was simply discarded with no attempt to check whether it had actually gone through. Now reconciles: checks the reference before giving up, completes the boost if it turns out the payment succeeded, returns quietly to plan selection with no alarming error if it was a genuine cancel.
- **Store-name change didn't propagate to existing listings** — mirrored web's already-proven reconciliation (re-sync each of the seller's own listings with the new name after the profile save succeeds), which mobile never had at all.

**Documented at the time, then built once Vincent asked for it:**
- **Mobile push notifications** — `expo-notifications`/`expo-device`/`expo-constants` added, client-side registration flow (`mobile/src/utils/pushNotifications.ts`, wired into `App.tsx` on sign-in), a new `POST /api/users/push-token` endpoint, and real push delivery wired into the existing `createNotification()` (every notification type — new message, new follower, followed-seller-new-listing — now also attempts a real push, not just an in-app row) via Expo's push service (no separate FCM/APNs server credentials needed). **Depends on a schema change not yet applied**: `.ai/handoffs/PUSH_NOTIFICATIONS_SCHEMA_PROPOSAL.md` has the exact SQL (`users.pushToken`/`pushTokenPlatform`/`pushTokenUpdatedAt`, three nullable columns, no grant/RLS implications) — awaiting Vincent's approval and execution, same discipline as every other schema change this session. Until that runs, every new code path fails gracefully (caught, logged, no-op) rather than erroring. Also still needs, after the schema change: a fresh EAS build (native module, doesn't work in Expo Go) and real-device verification — both outside what this sandbox can do.

**Still documented, not built** — requires new infrastructure or a product decision, not a fix-in-place:
- **Product-detail "Similar Listings" (and `ForYouListingsView`/`SellersToDiscover`) are capped/stale by construction** — they filter the same paginated `products` context array (max 24, most-recent-first) rather than a dedicated endpoint, so a detail page frequently shows "No similar items found" even when true matches exist further back in the catalog. Unlike the Featured/Trending cases, there's no existing dedicated endpoint being bypassed here — closing this properly means building one (e.g. a real `/api/similar` querying by category server-side across the full catalog), which is new API surface, not a redirect to something that already exists.

Every fix in this session: traced to an exact file/line before being acted on (not taken on an audit's summary alone), `tsc --noEmit` clean, web fixes additionally verified with a full production build, each committed as its own isolated, individually-reviewable unit and pushed immediately. Full commit list on `main` from this session: `84edf42`, `beff67b`, `52b6ffb`, `9eec009`, `1112a7c`, `5c775dc`, `7e138a5`, `79b7b9e`, `f6aae00`, `39998aa`, `a926988`, `8fd2261`, `daf05f6`, `a8d106c`, `1f691d9`, `54e3f75`, `5353168`, `314efe8`, `a793cff`, `23b184f`, `24fbc2b`, `f080638`.

---

**✅ CONFIRMED, 2026-09-16 — `service_role` migration (commit `4c1d667`) is live in production.** Vincent's Render deploy log shows `[Supabase Server] Initialized backend Supabase client: https://hnfqymkdgadwzrjenaqf.supabase.co (role: service_role)`, no warning line. Step 0 of the corrected rollout is done and verified — the remaining Phase 4 blockers are everything else in `RLS_ENABLEMENT_READINESS_REPORT.md`'s checklist, not this one anymore.

Two things surfaced by that same log, worth recording:

1. **The real production Supabase project is `hnfqymkdgadwzrjenaqf.supabase.co`, not `kxfykyxagkbrjymjmtal.supabase.co`.** The latter is `server.ts`'s hardcoded fallback (both URL and anon key) for what looks like an old/abandoned project reference — apparently never actually used in production (which always had real `SUPABASE_URL`/`SUPABASE_ANON_KEY`-equivalent env vars set), only ever reached by an environment lacking those vars. **This retroactively explains this whole session's "persistent Supabase DNS failure"**: this sandbox was never unable to reach Supabase in general — `hnfqymkdgadwzrjenaqf.supabase.co` resolves and responds fine (confirmed via a direct check after seeing this log). It was specifically trying to resolve the wrong, stale fallback host the entire time. This sandbox still doesn't have the real project's anon key (only Render does), so full anon-key live testing still isn't possible from here — but "no Supabase connectivity at all," as stated repeatedly throughout `RLS_ENABLEMENT_READINESS_REPORT.md` and this whole session, was not quite accurate. Worth a mental note when reading any earlier "couldn't verify, no connectivity" caveat in this session's docs: the real constraint was "no real credentials for the correct project," not "no network path."
2. **`[Supabase Server Products] summaryColumns select failed, trying select(*): column products.updatedAt does not exist`** — a real, currently-true schema/code gap, same shape as the `reports` table but **not broken**: this code path already has its own graceful fallback (falls back to `select(*)`, then `normalizeServerProductSummaryRow` derives `updatedAt` from `createdAt` or the current time if the column isn't present), and the surrounding comment shows this was already diagnosed and handled in an earlier pass, not something new. Product listings work correctly either way; the only consequence is `updatedAt` isn't a real persisted "last edited" timestamp for products today, it's a computed stand-in. Not investigated further or fixed — flagged for Vincent to decide whether it's worth adding the real column (low-risk, same shape as `reports_table_creation.sql`, an `ALTER TABLE public.products ADD COLUMN IF NOT EXISTS "updatedAt" TIMESTAMP WITH TIME ZONE;`) or leave it as the graceful fallback it already is.

---

**🚩🚩 MAJOR CORRECTION, 2026-09-16 — the server has never used a `service_role` Supabase key. This overturns a foundational assumption repeated throughout this entire session's RLS migration work.** Discovered while fixing the `reports` table below: granting `service_role` alone on the new table (per the original advice) left it broken in production with `permission denied for table reports`. Investigated `server.ts` directly — its one and only Supabase client, `backendSupabase` (209 call sites in the file), is built from `VITE_SUPABASE_ANON_KEY`/`SUPABASE_ANON_KEY`, with a hardcoded anon-role JWT as its fallback. `SUPABASE_SERVICE_ROLE_KEY` is referenced nowhere in this codebase — not in `server.ts`, not in `.env.example`. Fixed the immediate symptom by granting `anon`/`authenticated` on `reports` too, matching every pre-existing table's grants — confirmed working.

**Why this matters far beyond the one table**: `SUPABASE_RLS_MIGRATION_PLAN.md`'s own "foundational fact #4" stated the server's client "bypasses RLS entirely, by design, always" via `service_role`, and this was the load-bearing justification for the entire Phase 4 design (enable RLS on every table, zero policies, true default-deny) and for `RLS_ENABLEMENT_READINESS_REPORT.md`'s bottom-line "should break nothing" conclusion. **Both were wrong.** The server has been running on the same anon key as the browser's now-removed direct-Supabase path the whole time — it's just gated by a proxy (`verifyUser()`/`verifyAdmin()`, real Firebase-token checks) in front of it, not a more-privileged database credential. **If RLS had been enabled as originally recommended (zero policies, expecting `service_role` to be unaffected), every `server.ts` endpoint touching Supabase would have broken simultaneously** — not a `reports`-shaped edge case, the entire application.

**Both documents have been corrected in place** (prominent notices at the top of each, plus inline fixes at every specific false claim — foundational fact #4, the Phase 4 design section, the rollback strategy, the readiness report's §1/§2/§8) rather than quietly edited, so the error and its correction are both visible in the record. **New, blocking prerequisite added to the plan**: before Phase 4 can happen at all, `server.ts` needs to be migrated to a real Supabase `service_role` key (get it from the dashboard → Project Settings → API, set as a new env var, update `backendSupabase`'s initialization) — detailed as step 0 of `RLS_ENABLEMENT_READINESS_REPORT.md` §8. Not yet done. RLS remains untouched and `BLOCKED_APPROVAL`, now for an additional, more fundamental reason than before.

---

**🚩 ACTION NEEDED FROM VINCENT — `reports` confirmed missing in production; fixed.** (Superseded in part by the correction above — the missing grant this section describes fixing turned out to be a *symptom* of the server's anon-key status, not a `reports`-specific gap.) Vincent checked the Supabase dashboard directly: every other table this session's audits and migration work assumed exists does; `public.reports` does not. Root cause (repo archaeology, since this sandbox has no Supabase connectivity to have checked this directly itself):

- `supabase_schema.sql` is a hand-run "repair script" (its own header: *"Copy and paste this code into your Supabase SQL Editor"*), not a migration system. It's also almost certainly the actual origin of RLS being disabled project-wide — it ends with explicit `DISABLE ROW LEVEL SECURITY` + `GRANT ALL ... TO anon` for 9 tables.
- `reports` and `admin_audit_logs` are absent from *every* section of it (table creation, indexes, RLS-disable, grants) — not a partial oversight, a complete absence. (`admin_audit_logs` has not been separately confirmed against the dashboard; flagged for the same reason but not yet checked.)
- This file has been touched by exactly one commit in this repo's entire history (the initial squashed 161-file import, `8b9c494`, 2026-08-24) and never since, across ~184 commits of continued development. It's a frozen day-one snapshot, not a living record.
- `reports` has real origins in the app's *original Firestore-based* design — a full entity in `firebase-blueprint.json`, a complete owner-scoped security-rules block in `firestore.rules` — but `scripts/migrate-firestore-to-supabase.ts` (the Firestore→Supabase port) never references it at all; that script is explicitly scoped to product/media migration only. Most likely story: the Supabase-backed version of this feature was written assuming a table that was never actually created, rather than one created by hand and later undocumented.
- No "missing table" auto-heal exists anywhere in `server.ts` (only a missing-*column* auto-heal does) — `POST /api/reports/create` has been failing loudly (real Postgres error, logged server-side, 500 returned, client re-throws rather than swallowing) for however long this has been deployed. Neither handoff document has ever recorded a live success-path test for it, only rejection-path tests — this gap was masked by the same Supabase connectivity limitation that's blocked all positive-path testing all session.

**Ready-to-run script prepared, not applied by Claude — this sandbox has no Supabase connectivity or credentials at all, so running it is Vincent's action regardless**: `.ai/handoffs/reports_table_creation.sql`. Column set matches exactly what `dbAdapter.ts`'s `TABLE_COLUMNS.reports` and `server.ts`'s actual insert already use. Includes the `service_role` grant the table needs to function at all (a plain Postgres prerequisite, not an RLS question — omitting it would leave the feature broken with a more confusing error). Deliberately does **not** grant `anon`/`authenticated` or touch RLS either way — the script explains both options and leaves that one choice to Vincent.

---

**🚩 ACTION NEEDED FROM VINCENT — repo copy fixed (commit `7711db1`); the live Console rules still need manual publishing.** `firestore.rules` contained a hardcoded bypass string, `TEDBUY_SERVER_BYPASS_SECRET_2026_XYZ`, in the `products.update` and `boost_purchases.create` rules (letting a write skip the normal `isOwner`/`isAdmin` check entirely if the payload included it). This GitHub repo is public, so the secret was exposed since the root commit of this branch's history (`8b9c494`, 2026-08-24).

**Investigated exhaustively before touching anything** (full repo + all git history, every ref, 31 dangling objects, both unrelated `vercel/*` branches — see the git log around commit `bc38d8e` for the full write-up): the secret is never referenced by any application code anywhere (`server.ts`, `src/`, `mobile/src/`). `server.ts`'s real Firestore writes (mirroring `products` for mobile's real-time listeners) go through the Firebase Admin SDK, which bypasses security rules entirely and unconditionally by design — they never needed this bypass. `boost_purchases` gets zero Firestore writes from `server.ts` at all. Nothing legitimate depends on it.

**Fixed at commit `7711db1`**: removed the bypass condition from both rules outright — no replacement/rotated secret, since nothing legitimate ever used it and there's nothing to preserve. Verified by hand (no test tooling exists for this file): both edited blocks re-read in full context, plus a comment-aware parens/braces/brackets balance check confirming identical structure to the original file minus the two bypass branches.

**What I still cannot do from this sandbox — no Firebase CLI, no `gcloud`, no service-account credentials anywhere (checked `.env`, shell env, `~/.config`) — so this is a manual step for Vincent:** the corrected rules need to actually be pasted into the Firebase Console's Rules editor for the live project and published. Whether the Console's current live rules even match this repo's copy (i.e., whether this file was ever actually deployed at all) was never confirmed from this sandbox either way — check the Console directly. Either way, once confirmed live-or-not, the corrected version here is what should end up published.

---

**UPDATE — RLS migration IMPLEMENTATION: Phases 1 and 2 both fully complete.** `.ai/handoffs/SUPABASE_RLS_MIGRATION_PLAN.md`. Following the plan below (designed, not implemented, as of the previous update), 21 code checkpoints (5 through 25) were executed across several work sessions — each an isolated commit, `tsc --noEmit` + production build clean at every step, live rejection-path tests against a local server wherever a new or modified endpoint was involved, full diff review before every commit. **Every direct `setDoc`/`updateDoc`/`deleteDoc`/`writeBatch`/`getDoc`/`getDocs`/`onSnapshot` call against a real, in-scope table anywhere in `src/` has been migrated to an authenticated, ownership-checked server endpoint.**

Headline items, most consequential first:

- **`deleteProduct`'s direct `deleteDoc` (checkpoint 22) — the single most severe finding in this entire migration.** A direct, unauthenticated Supabase delete reachable with **zero authentication at all** (not even a login required), running redundantly right after an already-correct, already-ownership-verified server endpoint. Found only on a second, final re-sweep that widened its search from `AppContext.tsx` alone to every file in `src/` — a reminder (also true of checkpoints 10-11 and 15-16-25 below) that this migration's own earlier "final" sweeps had repeatedly understated their own completeness.
- **The account-migration merge (checkpoint 13)** — previously this document's own single highest-risk deferred item: `findAndMigrateExistingUser`'s cross-account merge used to delete another user's row based on nothing but a client-side search result. Rewritten around a new `POST /api/users/merge-account` endpoint that independently re-verifies ownership server-side (the old account's stored email must match the caller's own Firebase-verified email) before ever deleting anything — fails closed if it can't verify.
- **`updateProduct`'s three direct-write gaps (checkpoint 17)**, one of which allowed setting `boostStatus`/`boostEndDate`/`boostPriority` on ANY product for free, fully unauthenticated — completely bypassing a prior P0 fix (`trustBoostFields`) that had already closed this exact exploit at the server layer, because the direct write never went through server logic at all.
- **A second, previously-undiscovered bulk-PII-read vulnerability (checkpoints 10-11)** — the original Phase 0 sweep's own confidence that it had found "the" bulk-read issue (`fetchUsersOnce`) was itself incomplete; a second, unrelated instance existed inside the account-recovery search, missed because that sweep's grep only matched one specific call pattern.
- **`loginUser`'s username/phone-number query (checkpoint 16)** and an **admin-diagnostics self-test in `ProfileSettings.tsx` (checkpoint 25)** — both cases where an *earlier pass in this same migration* had explicitly assessed the finding as "no change needed" (once because it looked like "just a single targeted lookup," once because the UI gate was admin-only) and both assessments were wrong, since dbAdapter's generic write path has no ownership check regardless of what any one caller sends or which UI gates the button.

Also closed: the consolidated 5-site self-profile-reads finding (checkpoint 14, a realtime subscription converted to a 5s poll wrapped in a Firestore-doc-shaped shim so its large existing callback needed no changes); `updateUserProfile`'s two remaining blocks (checkpoint 18, folded a store-name-cleanup step into `POST /api/users/sync` itself); `setupWelcomePackage` and `sendWelcomeEmailToAll`'s writes (checkpoints 19-20, new `POST /api/welcome/setup` endpoint, later reused by checkpoint 24's `reportListing` fix too); and the `reviews` bulk read (checkpoint 21, the item that stayed open longest because `GET /api/reviews` genuinely needed an optional-`sellerId` mode, not a drop-in swap).

**Explicitly out of scope, noted rather than fixed**: `chat_typing` (an ephemeral, non-PII "who's typing" indicator never listed in this document's table inventory — negligible worst-case impact); `mobile/src/firebase.ts`'s 7 direct reads against real Firebase **Firestore** (a different security-rules system entirely, not Supabase RLS — a materially different investigation, flagged for its own future look, not started).

**Status: still BLOCKED_APPROVAL.** RLS itself, policies, grants, schema, and production config remain completely untouched across all 21 checkpoints — every fix routes traffic through the server's existing `service_role` access instead. Phase 3 (public reads) already has a standing recommendation (stay server-mediated); Phase 4 (enabling RLS) requires Vincent's explicit go-ahead before any policy work begins; Phase 5 (the verification test matrix) has not been run and needs a real Supabase connection this sandbox does not have.

---

**UPDATE — RLS migration design complete (read-only, no implementation).** `.ai/handoffs/SUPABASE_RLS_MIGRATION_PLAN.md` (new document). Requested as the next step after the sweep closed: design the safe path from `Web → dbAdapter → Supabase anon key` to `Web → Firebase Auth → authenticated TedBuy server API → server-side Supabase`, without touching RLS, policies, grants, schema, `dbAdapter.ts`, `server.ts`, or client code. Nothing was implemented — this is a design document only.

**Headline finding, not fixed (per this task's explicit no-implementation instruction):** building the read/write inventory surfaced a live, unauthenticated bulk PII exposure not covered by the completed sweep — `AppContext.tsx`'s `fetchUsersOnce` reads the **entire `users` table** directly from Supabase (`select('*')`, no auth, no column restriction), returning every user's email/phone/WhatsApp number/admin-status/suspension-status to anyone. A safe, purpose-built replacement already exists and is unused by web (`GET /api/users/list`, built for mobile, already strips PII). Flagged prominently in the plan (§0) as P0/P1-equivalent severity — recommend authorizing this as a fast, standalone fix rather than letting it sit through the full migration timeline, since it's a one-line call-site swap onto an endpoint that already exists.

**Core architectural finding:** confirmed via exhaustive grep that TedBuy has **no Supabase Auth session anywhere** — the browser only ever authenticates to Firebase, never to Supabase (`createClient(url, anonKey)` with zero `.auth.signIn*` calls found). This means `auth.uid()` in any RLS policy is always `NULL` for every browser request; no RLS policy can express per-user ownership without inventing a Firebase→Supabase JWT bridge (explicitly not proposed, per instruction — none currently exists). Consequence: the target RLS design is deliberately simple — default-deny on every table, zero policies, once all legitimate traffic is moved off the anon key entirely. RLS's only real job in this architecture is capping what the public anon key can do; per-user authorization remains, and must remain, the server's job.

**What the plan contains:** a full per-table inventory (10 tables, every read/write operation, current caller, current authorization, sensitivity, category A/B/C/D, existing-vs-new endpoint mapping, what breaks if RLS flips on today, priority); confirmation that most of the necessary secure server endpoints **already exist** and are simply unused by web (`/api/users/list`, `GET /api/reviews`, partial `/api/users/sync` coverage) — the same "infrastructure already exists, web wasn't migrated" pattern found repeatedly throughout the completed sweep; a 6-phase rollout (0: close remaining dangerous direct access — 1: protected writes — 2: protected reads — 3: confirm nothing needs to stay public via the anon key — 4: enable RLS, default-deny — 5: verification matrix across anonymous/normal/cross-user/admin identities); an explicit list of what breaks in production if RLS were flipped on today with no migration (registration, profile save, save/unsave a listing, likes/views, the user directory, admin support-desk inbox, message read-state); and a rollback strategy (RLS is a pure gate, not a data/schema change — `DISABLE ROW LEVEL SECURITY` is instant and safe if Phase 4 surfaces something Phase 5's verification missed).

**Recommendation:** the browser should end this migration talking to Supabase for nothing at all — `dbAdapter.ts`'s Supabase branch becomes dead code, not a smaller allow-list. Even the genuinely-public content (`products`, `reviews`) should stay server-mediated rather than gain an RLS-protected anon-read policy, because the existing server endpoints already do more (moderation filtering, caching) than a raw table grant would.

**Status unchanged: BLOCKED_APPROVAL.** No RLS, policy, grant, schema, `dbAdapter.ts`, `server.ts`, or client-code changes made in this pass. Waiting on Vincent's review of the plan and an explicit go-ahead before any implementation phase begins — including Phase 0, which touches client code even though it's framed as "prerequisite."

---

**UPDATE — final repository-wide client-trust / business-logic sweep complete.** `.ai/handoffs/SUPABASE_DIRECT_ACCESS_AUDIT.md` §19-22. This was requested explicitly as the closing pass before the RLS go/no-go decision — full field-by-field trace of every client-controlled value able to influence money, ownership, identity, admin/moderator privilege, suspension/deletion, moderation/visibility, trade completion, reviews/ratings, ranking/boosting, notifications, seller trust, privileged API behavior, or destructive operations, across both direct Supabase writes and authenticated server endpoints.

Four more real, distinct vulnerabilities found and fixed this pass, plus one prior audit-doc error corrected:

- **(P1) Username-quarantine bypass** (§20): the 90-day quarantine applied on account deletion (`store_names.status = 'quarantined'`) was written but never actually enforced at the real username-claim path (`/api/users/sync`) — only read by a pure UI-hint endpoint. A user could claim a just-deleted user's exact username (and its reputation) immediately. Fixed with a real server-side check; direct-Supabase route closed too (`status`/`availableAfter`/`quarantinedAt` stripped from `dbAdapter.ts`'s allow-list — no legitimate client write ever needed them).
- **(P1) Ranking manipulation via `/api/products/sync`** (§21.1): `viewsCount`/`likesCount`/`likedUserIds` were trusted as absolute client-supplied values, reachable against **any** listing (not just your own) via an intentional ownership-check bypass built for legitimate liking/viewing of someone else's listing. Both feed directly into ranking (`engagementScore`/`priorityScore`). Fixed: likes now only ever toggle the calling user's own id, the count is always derived from that array, views are preserved from the existing row unconditionally (no legitimate caller needs an absolute value here).
- **(P1) `reports` table completely unfiltered** (§21.2): missing from `dbAdapter.ts`'s `TABLE_COLUMNS` entirely (every other table has an entry) — a genuine oversight, not a deliberate gap, and it corrects an earlier (wrong) claim in this document's own §2 that assumed a missing entry caused a safe no-op. It didn't — writes passed through completely unfiltered. `reportProduct()` also spoofable client-side (`reporterId` from local state). Fixed by migrating to the already-existing, already-correct `POST /api/reports/create`, plus full write-barriers in `dbAdapter.ts`.
- **(P2) `reviews` write-then-null gap** (§21.3): no direct-write caller remained after an earlier pass's migration to `/api/reviews/create`, but a bare `setDoc(doc('reviews', id), {})` could still null an existing review by id. Closed with the same write-barriers as `reports`.
- **(Hardening) `emailVerified` client-trust in `/api/users/sync`** (§19): now derived from Firebase Admin SDK's own record instead of trusted from the client body — unlike `isAdmin`/`isSuspended`, simply preserving the existing DB value would have broken the legitimate "just verified my email" transition, so a different fix shape was needed here.

**Explicitly documented, not fixed** (§22) — same RLS-dependent residual-risk bucket as the rest of this document, each requiring new endpoint infrastructure or a product decision beyond an unambiguous fix-in-place: `viewsCount`/`likesCount`/`likedUserIds` remain writable via the direct-Supabase route (`toggleLikeProduct`/`incrementProductViews` depend on it, including anonymous view-tracking — `dbAdapter`'s `increment()` helper was confirmed to provide no real server-side atomicity, purely a client-side convention); `notificationPreferences` has no per-row ownership check on the generic write path (low severity — a muting/griefing nuisance, not privilege/money/identity); a `sellerVerified` display bug was found (`|| true` makes the "verified seller" badge unconditionally true for every seller) but is a business-logic anomaly with zero client input involved, out of this audit's scope, not fixed.

**All categories from the sweep request traced to actual effect, not just field-name matching** — full list and per-category findings in §22. Tested: `tsc --noEmit` clean, production build clean, rejection-path tests executed against a local dev server for every new/changed endpoint behavior. Diffs reviewed in full before each commit. Commits: `b9b2c86` (username quarantine + emailVerified), `839da5f` (reviews/reports/ranking-manipulation fix).

**This closes the sweep. Per Vincent's instruction, the RLS migration decision is next — waiting on his review of this report before any RLS work begins.**

---

**UPDATE — notification security layer BUILT, revising the previous "not fixed" assessment.** `.ai/handoffs/SUPABASE_DIRECT_ACCESS_AUDIT.md` §18.

§17 said closing notifications properly required building new server infrastructure and deferred it. Actually building it turned up something important: **most of the server infrastructure already existed** — `GET /api/notifications`, `POST /api/notifications/{mark-read,mark-all-read,clear-all}`, and `POST /api/users/follow` were already correctly built (real `verifyUser()` auth, real `.eq('userId', verified.uid)` ownership scoping) and **mobile already used all of them**. Web just never adopted them and kept using the old insecure direct-Supabase path in parallel. So this was substantially a migration task, not a from-scratch build:

- **Migrated**: web's notification read (was a live Supabase Realtime subscription with no server-side identity check on its own filter — now a 20s poll of the real, authenticated endpoint; real UX tradeoff, no more instant push, documented as deliberate), mark-read/mark-all-read/clear-all, and follow/unfollow — all now call the real server endpoints instead of writing directly.
- **Removed as redundant**: `sendMessage`'s and `createProduct`'s own direct notification broadcasts — both already had an equivalent, real, server-authoritative notification firing automatically via `/api/messages/send`/`/api/products/sync`, so the client-side versions were duplicating (insecurely) work the server already did correctly.
- **Genuinely built new**: server-side "listing updated" notifications (no prior equivalent existed — added to `/api/products/sync`, mirroring its own existing new-listing notification pattern exactly), and a narrow admin-only fallback in `/api/messages/send` so an admin can reply as the CEO-support pseudo-account server-side (`verified.isAdmin`-gated, never client-claimed) — this was the one case with no existing endpoint, and building it was what allowed **fully** closing the vulnerability rather than leaving one insecure exception open.
- **Closed for good**: `dbAdapter.ts` no longer maps `'notifications'` to anything at all — not just an emptied allow-list, the table mapping itself is gone, so there's no direct-Supabase route left for anyone to use, confirmed by a repo-wide grep finding zero remaining client-side notification writes.
- Every notification type traced had an unambiguous, checkable server-side event to verify (follow → real followingSellers check; message → real chat-participant check; listing new/updated → real ownership + follower/saved-user data; admin-support → real cryptographic admin check) — nothing was invented or guessed at.

**Testing**: 13 rejection-path tests executed live (all pass) plus a static confirmation the direct-Supabase route is gone; `tsc`/build clean. Explicitly did NOT fabricate the identity-dependent tests requested (two real accounts, a real admin, a real non-admin) — no test-credential infrastructure exists for that here, and it's flagged plainly rather than faked.

---

Any client can create a notification targeting an arbitrary victim's `userId` with fully attacker-controlled title/message/sender-name (impersonation/phishing risk — a fake "TedBuy Official" notification, delivered into a real user's inbox), and can mark-read or delete another user's notifications, since all notification writes go through the unauthenticated `dbAdapter` path with zero server-side logic at all. **Why this wasn't fixed like everything else**: every prior fix this session closed a gap in logic that already existed server-side (patch what's there, stop trusting the wrong field). Notifications have no server-side logic to patch — properly closing this means designing and building new API surface with **per-notification-type validation** (a "new follower" notification is only legitimate if the server can verify the caller really just followed that seller; a "new message" notification only if they're a genuine chat participant, etc.) — a materially bigger, higher-risk undertaking than a focused patch, on a live feature five real call sites depend on today. Classified P1 (real impersonation/privacy risk, not financial/account-takeover). Documented with a concrete recommended build order (mark-read/delete endpoints first — simple ownership checks, low risk; creation endpoint with per-type validation second) rather than guessed at or rushed. Continuing to the next read-only area.

**UPDATE — product moderation/visibility audit complete, one real fix made.** `.ai/handoffs/SUPABASE_DIRECT_ACCESS_AUDIT.md` §16.

- **`status` self-reinstatement (P1, fixed)**: `upsertProductToSupabase` (used by `/api/products/sync`) unconditionally honored a client-supplied `status: 'active'` claim. A listing moderated to `'archived'`/`'hidden'`/`'deleted'` could be self-reinstated by its own owner via the ordinary edit-listing flow. Fixed: non-admin callers can now only move a listing between `active`/`sold` (unchanged Mark as Sold behavior — verified with 11 executed test cases against the exact logic now live in the code, not just traced by eye), and never out of a moderation-locked state; admins retain full authority.
- **`isDeleted`/`archivedAt`/`securityHold`/`isApproved` on products** — same direct-Supabase exposure pattern as every prior fix this session (no per-row ownership check, RLS disabled). Closed the same way. `isApproved` specifically was confirmed **inert today** (no read path gates on it) — fixed defensively anyway, same reasoning as `emailVerified` in the previous pass: harmless now, becomes a real gap the moment a future feature wires moderation logic to it.
- **Important context discovered**: there is currently no admin product-moderation feature anywhere in the app — no endpoint, no UI, client or server. This isn't itself a vulnerability; it means today's fix is defensive/future-proofing for most of these fields (only `status`'s self-reinstatement path was concretely exploitable *today*, and only once a product's status is ever moderated via direct DB access).
- Also swept for other client-controlled money/permission/trust/ranking fields beyond the named list: nothing new found. `viewsCount`/`likesCount` remain client-influenceable within existing bounds (flagged, not fixed — low severity, already bounded by the ranking algorithm, out of this pass's specific scope). "Featured" listings confirmed to be entirely boost-derived (no separate flag), already covered by the previous pass's fix.

**Five P0/P1 vulnerabilities closed this session total, all fixed, tested to the extent safely possible, and committed.** Full detail: `.ai/handoffs/SUPABASE_DIRECT_ACCESS_AUDIT.md` §13-16.

- **Free, unlimited boost activation** — `server.ts`'s `upsertProductToSupabase()`, called by `/api/products/sync` (the ordinary "save my listing" endpoint every seller already uses), trusted `boostStatus`/`boostExpiry` straight from the client request body. Confirmed these are exactly the fields the real ranking computation (`getServerBoostEndDate()`) reads to decide whether a listing is actively boosted. **Any seller could grant themselves a free, arbitrarily-long, ranking-relevant boost via one ordinary API call — no payment, and no RLS bypass needed at all**, since this endpoint already runs through the service-role Supabase client. This is more severe than the RLS-disabled findings below it: it works through the app's own primary, already-authenticated API, not a workaround.
- **Unlimited payment-reference replay** — `/api/verify-payment` never checked whether a `paymentReference` had already been used. Since Paystack's verify endpoint is idempotent (reports "success" forever for a real past transaction), one payment could be replayed to indefinitely extend the same boost or activate boosts on multiple listings. A `boost_purchases` table already existed in the schema for exactly this purpose but nothing ever wrote to it.
- **Direct-Supabase route was open too** (defense-in-depth) — same boost fields were also in `dbAdapter.ts`'s client write-allowlist; closed the same way as every prior fix this session.
- **Correctness bug fixed alongside**: genuinely paid boosts were silently losing their plan/amount/history metadata on every subsequent listing edit, since the write path never carried those fields through at all (only `boostStatus`/`boostExpiry`) — now all boost fields properly persist for the two legitimate, trusted call sites (`/api/verify-payment`, `/api/admin/boost-control`).
- Confirmed safe (no fix needed): a client can't inflate the boost amount (server/Paystack-derived), can't boost someone else's listing (ownership-checked), and can't bypass expiry (recomputed fresh from stored dates on every read, no tamperable flag).
- The Paystack-secret-missing fallback ("demo mode") was re-inspected: deterministic, per-request, unreachable in production given the key is confirmed configured — the new replay protection also now applies to that branch regardless.

**Four P0/P1 vulnerabilities closed this session total, all fixed, tested to the extent safely possible, and committed.** Full detail: `.ai/handoffs/SUPABASE_DIRECT_ACCESS_AUDIT.md` §13-15.

1. **Self-promotion → admin account deletion/suspension** (§13): `POST /api/users/sync` let any authenticated user set their own `isAdmin` to `true` via a normal profile-save request — independent of RLS. Combined with two `AppContext.tsx` functions that never re-verified admin status server-side, this allowed full account deletion/suspension of any user. Fixed with two new server endpoints (`/api/admin/users/suspend`, `/api/admin/users/delete`), a server-side fix so `/api/users/sync` never trusts client-supplied `isAdmin`, and a `dbAdapter.ts` write-allowlist fix closing the direct-Supabase route too.
2. **isSuspended self-unsuspend** (§14.1): identical shape and fix to #1 — `/api/users/sync` also wrote `isSuspended` straight from the client body, letting a suspended user clear their own suspension. Fixed the same way. Also found (not fixed — needs a product decision, not a mechanical one): suspension is enforced **entirely client-side** — no server endpoint checks `isSuspended` before processing a request, so a suspended user's token still works everywhere regardless.
3. **Chat tradeStatus fraud** (§14.4, found via the Priority 3 sweep, not originally in scope): web's `markAsDelivered`/`markAsPickedUp` wrote `tradeStatus` directly via the unauthenticated dbAdapter path, and `/api/reviews/create` trusts `tradeStatus === 'completed'` as proof of a real trade — meaning anyone could fabricate review eligibility without ever transacting. Mobile already had the correct fix (calls real, already-authorized server endpoints); web just never used them. Fixed by switching web to the same endpoints.

Also fixed: **`/api/send-welcome-email` had no authentication at all** (§14.2) — real callers already sent tokens the server never checked; anyone could email arbitrary addresses a TedBuy-branded "welcome" email. Fixed: real auth required, non-admin callers restricted to their own verified email, admin bulk-send restricted to real registered users looked up server-side. A dead, unreachable 308-line duplicate registration of the same route was also removed.

**Full Priority 3 field-by-field classification also done** (§14.3) — `securityHold`/`status`/`isDeleted`/etc. had the identical unfixed vulnerability shape as isAdmin/isSuspended (no legitimate client write path existed at all, so fixed with zero functional risk). `role`, `isGoogleAuth`, `welcomeSent`, and other preference fields confirmed genuinely safe to leave client-editable. Two items flagged but intentionally not fixed this pass: `emailVerified` (low severity, self-only, no real secret protected) and boost-related product fields (not yet re-examined for a parallel bypass — UNKNOWN).

**Everything else remains open and BLOCKED_APPROVAL — none of this session's fixes touch RLS.**

---

**STATUS: BLOCKED_APPROVAL** (for the broader Supabase RLS migration — §1-11 of the audit doc)

**Reason:** Production Supabase RLS is confirmed disabled (verified by Vincent directly in the Supabase dashboard). Direct client access must be fully mapped before any security architecture change (re-enabling RLS, changing grants/policies, migrating write paths) is made. No production database, RLS, grants, policies, or `dbAdapter.ts`/`server.ts` code has been touched.

**Reference documents:**
- `.ai/handoffs/SUPABASE_DIRECT_ACCESS_AUDIT.md` — complete read-only inventory of every direct client-to-Supabase code path, classified by risk, mapped against existing authenticated server APIs, with a proposed 3-phase migration plan. This is the document to work from before implementing anything.
- `.ai/handoffs/vercel-audit.md` — separate, unrelated, also read-only/BLOCKED_APPROVAL-adjacent (legacy Vercel file cleanup pending Vincent's go-ahead).

**UPDATE — second pass found something more severe than the original RLS gap.** Tracing the account-deletion cascade (as requested) surfaced a real privilege-escalation chain, not just an authorization gap dependent on RLS alone:

- `adminDeleteUserProfile()` and `adminToggleUserSuspension()` (`AppContext.tsx`) are gated **only** by a client-side `currentUser.isAdmin` check — no server round-trip re-verifies admin status before they run (contrast `adminToggleSecurityHold` in the same file, which correctly does call a real server endpoint).
- `users.isAdmin` is a plain, client-writable column with no code-level protection — with RLS off, any actor can set their own row's `isAdmin` to `true` via a direct Supabase REST call (no TedBuy app code needed, just the public anon key + table/column names, both effectively public).
- Chaining these: anyone can self-promote to admin client-side, then use `adminDeleteUserProfile`'s real, reachable UI button (`ProfileSettings.tsx:3277`) to hard-delete (or suspend) **any other user's entire account** — products, reviews, chats, messages, store name, profile.
- Bonus, separate finding: the admin PIN second-factor (`verifyAdminPIN`) is pure client-side JS with a hardcoded `'2330'` fallback that always works — not a real gate either, though it doesn't even come into play for the two vulnerable functions above since neither checks it.
- Reviews audited end-to-end: creation is already properly server-verified (real trade/chat validation, no direct-write path) — no issue there. No update path exists. Delete only happens via the same vulnerable admin cascade.

Full detail, code references, and the exact exploit chain: `.ai/handoffs/SUPABASE_DIRECT_ACCESS_AUDIT.md` §12.

**What happens next:** waiting on Vincent to decide relative priority between (a) the §12.3 privilege-escalation fix (arguably more urgent than the broader RLS migration, since it doesn't require RLS to be re-enabled to matter — it needs `adminDeleteUserProfile`/`adminToggleUserSuspension` migrated to real server endpoints, and `users.isAdmin` writes blocked from ever reaching a client-controlled payload), (b) starting Phase 1 of the Supabase RLS migration plan (§9), or (c) commissioning the remaining open unknowns (admin_audit_logs/account_deletion_audits RLS status, `resetChats` reachability — still unresolved, full onSnapshot read-path inventory).

**Do not enable RLS, run `supabase_policies.sql`, change grants, modify `dbAdapter.ts`, `server.ts` authorization, or touch the admin-gated functions in `AppContext.tsx` until Vincent explicitly authorizes the implementation phase.**

Autonomous audit of unrelated, safe areas continues in parallel — see session commits on `main` (security fixes `7e1d89c`, video-feed perf `9bfebe7`, Vercel audit `03101bb`) for work already shipped this session.
